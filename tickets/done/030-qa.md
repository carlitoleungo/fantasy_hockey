## QA Report — 030

**Ticket:** Week Projection matchup fragment (compute + render)
**Engineer handoff:** tickets/030-done.md
**QA date:** 2026-07-03

### Test plan (written before reading Engineer's "How to verify")
- AC1: GET `/projection/matchup?my_team=<key>` (and `?team_key=<key>`) → assert 200, body has no `base.html`/`<html>`, exact context line `Week {w} vs {opp} · {start} – {end}`, and three tally cards (my wins / ties / opp wins). Fail = wrong status, full page returned, or missing card/line.
- AC2: Render the category table → assert one row per enabled stat, per-cell winner highlight class present, values match `compare_projections`. Also verify the highlight lands on the *correct* column for an opp-win and that a tie has no green (gap in Engineer's tests). Fail = highlight on wrong column, missing row, or wrong value.
- AC3: Assert both teams' roster breakdowns render, each player's remaining games + projected per-stat contribution present. Fail = one team missing or no per-player numbers.
- AC4: Bye team (no matchup pairing) → assert 200, "no matchup this week" message, no comparison/roster tables. Fail = 500, or tables rendered.
- AC5: Assert rate stat (GAA) 2dp, counting stat (Goals) 1dp, `_is_rate_stat` drives it, no rate stat summed (weighted avg). Fail = GAA summed or wrong precision.
- Bulk: assert `get_all_teams_week_stats` called exactly once (single `teams/stats`), lastmonth via batched `get_players_lastmonth_stats` (no per-player loop).
- Cross-cutting: dual param handling; whether any AC needs a live browser.

### Test results

| # | Acceptance criterion | Result | Observation |
|---|----------------------|--------|-------------|
| 1 | 200 HTML fragment (no base.html), context line + 3 tally cards | PASS | `test_matchup_context_line_and_tally_cards` asserts 200, `base.html` and `<html` absent, exact line `Week 14 vs Beta · 2026-03-23 – 2026-03-29`, `Projected Wins` count==2 + `Tied`. Template lines 1–29 confirm bare fragment. Both `?team_key=` and `?my_team=` return 200 (`test_matchup_my_team_alias`). |
| 2 | Category table, one row/enabled stat, per-cell winner highlight matching `compare_projections` | PASS | `test_matchup_category_table_winner_highlight` asserts `bg-green-100` + values 11.0/6.0. Engineer's test only checks green exists in the my-team-wins case; my supplementary `test_opponent_win_highlights_opponent_column` confirms green lands on the opponent cell (25.0) and my cell is `text-gray-400` (5.0) when opp wins, and `test_tie_category_has_no_green_highlight` confirms a tie leaves both cells un-highlighted. Template lines 44–61 map winner→column correctly. |
| 3 | Per-team roster breakdown (both teams), remaining games + projected per-stat | PASS | `test_matchup_roster_breakdown_both_teams`: `Roster Breakdown`, `My Skater`, `Opp Skater`, `>3<` (EDM), `>2<` (BOS) all present. Template renders two `roster_table` macros (lines 106–110) with GR column + per-stat cells. |
| 4 | Bye → 200 + "no matchup this week", no table | PASS | `test_matchup_bye_week_message`: team T3 has no scoreboard pairing → 200, "no matchup this week" present, `Roster Breakdown` absent. Route lines 114–123 short-circuit with `has_matchup=False`. |
| 5 | Rate 2dp / counting 1dp; `_is_rate_stat` drives; no rate summed | PASS | `test_matchup_rate_vs_counting_formatting`: GAA weighted rate `2.50` (2dp), Goals projected `11.0` (1dp), breakdown Goals contribution `6.0`. `project_team_stats` (analysis/projection.py:53–83) computes rate stats as a weighted average, never a sum; `_player_breakdown` (projection.py:73–77) shows rate stats directly. `is_rate_stat` passed to template and drives `fmt` (template line 45) and breakdown format (lines 91–95). |

### Bulk-call conformance
PASS. `test_matchup_uses_single_bulk_team_stats_call` asserts `get_all_teams_week_stats` `call_count == 1` — team week stats come from one `teams/stats` bulk call, not N per-team calls. Last-30-day stats use `players.get_players_lastmonth_stats` which batches internally in chunks of 25 (data/players.py:117–122) — no per-player Yahoo call. `get_remaining_games` is a single bulk call over all abbrs (data/schedule.py). Route has no per-entity Yahoo loop. Conforms to DECISIONS.md 2026-05-30 "per-stat bulk fetch, never per-player" and LEARNINGS "Bulk endpoints over per-entity loops".

### LEARNINGS gotchas
- GAA (`stat_id==23`) lastmonth recompute: handled inside `data/players.py`; the route does not re-touch it (uses the returned dict as-is). Not undone. PASS.
- `stat_id==0` games-played: enabled-stat list is built from `is_enabled` categories only (route line 92); games-played is not a category here. PASS.
- Enabled-only filtering via `is_enabled`: route line 92 filters on `c["is_enabled"]`. PASS.

### Automated tests
- Command: `python3 -m pytest tests/ -q` (via `.venv/bin/python -m pytest tests/ -q`)
- Before my additions: **372 passed, 0 failed** — independently reproduced the Engineer's claimed count.
- After my additions: **374 passed, 0 failed**.
- New tests added: `tests/test_projection_matchup_qa.py` (2 tests: opponent-win highlight lands on the correct column; tie applies no green). These close the AC2 "correct-column" inspection gap; all pass.

### Manual verification
No live-browser walk performed. Two reasons, both explicit:
1. No Yahoo OAuth credentials/tunnel available in this environment (same constraint the Engineer noted).
2. Per LEARNINGS.md "Off-season → no live data": we are in the NHL off-season (date 2026-07-03), so an authenticated live visit to this week-keyed page can *only* exercise the empty/placeholder state — it cannot validate real compute against live data regardless of OAuth. Demo mode is the documented answer for a visual check, and that is ticket 031 (out of scope here).

Instead, the templates are exercised end-to-end through the real FastAPI + Jinja stack (TestClient renders the actual `_matchup.html`), which asserts every AC's rendered output: context line, tally cards, winner-highlight classes and correct column, both roster breakdowns, bye message, and rate/counting precision.

### Verification-gap determination (owner-flagged item 2)
**No acceptance criterion requires manual/visual browser inspection.** All five ACs are observable in rendered HTML and are covered by passing render tests plus code inspection. The only things not verified are purely presentational and are NOT acceptance criteria: whitespace/spacing, the `sticky left-0` column's actual pinned scroll behaviour, and `overflow-x-auto` horizontal scrolling on a narrow viewport. The corresponding Tailwind classes are present in `_matchup.html` (lines 34, 70–71, 74, 84), so the markup is in place; their visual rendering is owner-must-verify (ideally via demo mode / ticket 031) but does not gate this ticket.

### Dual query-param determination (owner-flagged item 1)
**Acceptable — not a defect.** Verified both param names return 200 and drive identical behaviour: `?team_key=<T1>` (all AC tests) and `?my_team=<T1>` (`test_matchup_my_team_alias`) both render `Week 14 vs Beta`. Route line 218 uses `selected = team_key or my_team`. This reconciles a real contradiction: AC1's literal URL uses `my_team`, but the immutable 029 shell (`web/templates/projection/index.html`, out of scope for 030) sends `team_key` in both the `<select name="team_key">` (line 23) and the auto-load `hx-get="...?team_key=..."` (line 31). Accepting both is the only way to satisfy the AC's literal URL and the immutable shell without editing an out-of-scope file. Recommend a follow-up (shell ticket) to converge on a single param name so the interface isn't permanently dual; logged as a note, not a blocker.

### Demo mode
N/A for this ticket — 030 adds no new `data/`/`analysis/` functions (it composes existing ones) and the demo route `/demo/projection/matchup` is explicitly ticket 031. `_matchup.html` contains no hardcoded route URLs (verified — it references no endpoints), so it is reusable by 031 unchanged, as the ticket requires.

### Issues found
None (no FAIL). One non-blocking note carried above: the dual param name is a pragmatic reconciliation that should eventually be collapsed to one name via a 029-shell follow-up.

### Verdict: APPROVED
