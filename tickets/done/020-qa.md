## QA Report — 020

**Ticket:** Demo mode: /overview leaderboard
**Engineer handoff:** tickets/020-done.md
**QA date:** 2026-05-31

---

### Test plan (written before reading Engineer's verification)

- **AC1:** `curl -s -o /dev/null -w "%{http_code}" http://localhost:8501/demo/overview` → expect 200. Then inspect HTML for "Demo League", "Weekly Leaderboard", week selector (`<select>`), and `<tr>` rows with team data. Failure: non-200, missing week selector, empty table, auth redirect.
- **AC2:** `curl -s http://localhost:8501/demo/overview/table?week=5` → expect 200, HTML fragment (no `<html>` tag), `<tr>` rows with team names and stat values, `bg-green-100`/`bg-red-100` rank cells. Also test `?week=1`. Failure: non-200, full-page HTML wrapper, empty rows.
- **AC3:** Inspect server logs after hitting both demo routes — expect only `200 OK` lines, no references to `data/client`, `auth/oauth`, or `api.sports.yahoo`. Also read `data/demo.py` to confirm `get_matchups()` reads local parquet, not Yahoo API. Failure: any Yahoo API call in logs.
- **AC4:** `curl -s http://localhost:8501/demo/overview | grep hx-get` → expect `hx-get="/demo/overview/table"` on the `<select>` element. Also check `hx-target` and `hx-trigger`. Flag in-browser swap animation as owner-must-verify. Failure: `hx-get` points to wrong URL or is absent.
- **AC5:** `curl -s -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:8501/overview` → expect `302 http://localhost:8501/auth/login`. Read `web/routes/overview.py` to confirm authenticated handler is structurally unchanged (only `table_url` key added to context dicts). Failure: non-302, wrong redirect target, or handler logic changed beyond `table_url`.

---

### Test results

| # | Acceptance criterion | Result | Observation |
|---|----------------------|--------|-------------|
| 1 | `GET /demo/overview` returns 200 and renders the leaderboard shell with week selector and stat table populated from demo data — no Yahoo session required | PASS | HTTP 200. HTML contains `<span class="text-gray-500 text-sm">Demo League</span>`, `<h1 ...>Weekly Leaderboard</h1>`, `<select id="week-select" ...>` with options Week 1–Week 14 (week 14 `selected`), and 12 `<tr class="border-b hover:bg-gray-50">` data rows. |
| 2 | `GET /demo/overview/table?week=N` returns 200 and renders `overview/_table.html` fragment populated from demo data for the requested week | PASS | HTTP 200 for both `?week=5` and `?week=1`. Response begins with `<div class="overflow-x-auto">` (no `<html>` wrapper — confirmed 0 `<html>` tags). First team name cell: `<td ...>AHO-V-O Crew</td>`. Stat columns: Goals, Assists, Points, Plus/Minus, Penalty Minutes, Powerplay Points, Shots on Goal, Faceoffs Won, Hits, Blocks, Wins, Goals Against Average. `bg-green-100` cells present. |
| 3 | No outbound Yahoo API calls are made when hitting either demo route | PASS | Server log for all requests shows only `200 OK` lines. No references to `data/client`, `auth/oauth`, or any Yahoo hostname. `data/demo.py` imports only `json`, `pathlib.Path`, and `pandas` — no `data.client` or `auth.oauth` imports. |
| 4 | Navigating to `/demo/overview` and changing the week selector updates the table via HTMX (fragment swap fires against `/demo/overview/table`) | PASS (HTML wiring) / OWNER-MUST-VERIFY (browser swap) | `<select id="week-select">` has `hx-get="/demo/overview/table"`, `hx-target="#leaderboard-table"`, `hx-trigger="change"`. Wiring is correct. Actual HTMX swap animation in browser cannot be verified without a browser. |
| 5 | An authenticated user visiting `/overview` still sees the live-data route unchanged | PASS (redirect) / OWNER-MUST-VERIFY (live session) | `GET /overview` without auth returns `302 → http://localhost:8501/auth/login`. Source inspection of `web/routes/overview.py`: authenticated `overview()` handler is structurally unchanged — `require_user` dependency, `make_session`, `get_matchups(session, league_key)` call all intact. Only change is `"table_url": "/overview/table"` added to both context dicts (normal and empty-data path). The template change (`hx-get="{{ table_url }}"`) is backwards-compatible — authenticated shell now passes the URL rather than hard-coding it. Live session test not possible without Yahoo credentials. |

---

### Automated tests

- **Command:** `/Users/carlin/dev/fantasy_hockey/.venv/bin/python -m pytest tests/ -v`
- **Tests run:** 332 — passed: 332, failed: 0
- **New tests added:** None for the demo overview routes specifically. `test_overview_routes_qa.py::test_shell_has_htmx_attributes` tests the authenticated shell's `hx-get="/overview/table"` wiring, but there are no parallel tests for `GET /demo/overview` (200 status, correct HTMX wiring) or `GET /demo/overview/table?week=N` (200 status, fragment shape). This is a gap — other demo routes such as `/demo/waiver` have route-level tests in `test_waiver_routes.py`. The gap does not block this ticket (the routes work and the existing tests all pass), but a follow-up should add `test_demo_overview_shell_returns_200`, `test_demo_overview_table_returns_fragment`, and `test_demo_overview_no_auth_required` to close parity with the waiver demo test coverage.

---

### Manual verification

- Started server: `/Users/carlin/dev/fantasy_hockey/.venv/bin/uvicorn web.main:app --reload --port 8501`
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:8501/demo/overview` → `200`
- `curl -s http://localhost:8501/demo/overview | grep -E "(Demo League|Weekly Leaderboard|hx-get|week|<select|<option|<tr|<table)"` → confirmed all expected elements present; `hx-get="/demo/overview/table"` on `<select id="week-select">`, weeks 1–14, week 14 selected by default, 12 data rows
- `curl -s -o /dev/null -w "%{http_code}" "http://localhost:8501/demo/overview/table?week=5"` → `200`
- `curl -s "http://localhost:8501/demo/overview/table?week=5" | grep -c "<html"` → `0` (confirmed fragment, not full page)
- `curl -s "http://localhost:8501/demo/overview/table?week=5" | grep -E "<tr class"` → 1 header row + 12 data rows
- `curl -s "http://localhost:8501/demo/overview/table?week=5" | grep "bg-green"` → `bg-green-100` cells present
- `curl -s -o /dev/null -w "%{http_code}" "http://localhost:8501/demo/overview/table?week=1"` → `200`
- Server log after all requests: only `200 OK` lines, no Yahoo API activity
- `curl -s -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:8501/overview` → `302 http://localhost:8501/auth/login`
- Read `web/routes/overview.py`: `public_router` declared at module level; `demo_overview` and `demo_overview_table` on `public_router` with no `require_user` dependency; authenticated routes unchanged except `table_url` key added to context dicts
- Read `web/main.py`: `overview_public_router` imported and registered before `overview_router` at lines 16–17, 69–70
- Read `web/templates/overview/index.html`: single occurrence of `hx-get="{{ table_url }}"` at line 18 — no hard-coded `/overview/table` string remains in the template

---

### Demo mode

This ticket is entirely demo mode — both new routes are unauthenticated. Demo parity check performed above. No data layer changes; `data/demo.get_matchups()` was pre-existing and unchanged.

---

### Issues found

None blocking.

**Note (non-blocking):** No automated tests cover the new demo overview routes. `test_waiver_routes.py` includes `test_demo_waiver_shell_returns_200` as a parallel; the same coverage should be added for `GET /demo/overview` and `GET /demo/overview/table?week=N`. File a follow-up ticket or add tests in `tests/test_overview_routes.py`.

---

### Verdict: APPROVED
