## QA Report — 032

**Ticket:** Waiver wire multi-position filter
**Engineer handoff:** tickets/032-done.md
**QA date:** 2026-07-21

### Test plan (written before reading Engineer's verification)

- **AC1** — `GET /demo/waiver` → 200. Grep the position control: expect
  `<input type="checkbox" name="positions" value="X">` for each of C/LW/RW/D/G (plus All).
  Failure: any `type="radio" name="positions"`, or any legacy `name="position"` field.
- **AC2** — `POST /demo/api/waiver/players` with `positions=C` + `positions=LW` + a stat.
  Parse the `display_position` of every row across all pages. Expect every player's
  position to intersect {C, LW}. Failure: any row whose position is pure RW/D/G.
- **AC3** — `POST` with `positions=D` + `positions=G` + a stat. Scan all pages of the
  union. Expect at least one D-eligible and at least one G-eligible player, and zero
  players outside {D, G}. Failure: no goalie anywhere in the union, or a non-selected
  position leaks in.
- **AC4** — `POST` with no positions, and separately `positions=All`. Expect the full pool
  spanning >1 position group (C/LW/RW/D/G) and identical results for both. Failure: filtered
  to a subset, or the two forms diverge.
- **HTMX/reset** — shell markup must carry `hx-trigger="change"` and reset `#page-input` to 0
  on change.

### Test results

| # | Acceptance criterion | Result | Observation |
|---|----------------------|--------|-------------|
| 1 | `GET /demo/waiver` renders `type="checkbox" name="positions"` for C/LW/RW/D/G (not radios) | PASS | 200. Position control emits exactly 6 checkboxes: `value="All"` (checked) + C/LW/RW/D/G, all `type="checkbox" name="positions"`. Zero `type="radio" name="positions"`; zero legacy `name="position"`. |
| 2 | `positions=C`+`LW`+stat → every listed player eligible at C or LW | PASS | Real demo data, all 84 rows across all 4 pages: 0 players lacking a C/LW overlap. Rows like `C,RW` and `LW,RW` correctly included (share C/LW); no pure RW/D/G. Superset check: C alone=54, LW alone=51, C+LW union=84 (deduped on dual-eligibles). |
| 3 | `positions=D`+`G` → union with both a D and a G in same table | PASS | 97-row union across all pages: 53 D-eligible, 44 G-eligible, 0 outside {D,G}. Goalie present (e.g. "Stuart Skinner", G). Non-selected positions (C-only) absent. See ranking note below on page-1 composition. |
| 4 | No positions (or `positions=All`) → players span >1 position group (default preserved) | PASS | No-positions POST returns full 199-row pool spanning all 5 groups (C=54, RW=45, LW=51, D=53, G=44). `positions=All` returns the identical 199 rows. |
| — | HTMX `change` auto-refresh + reset to page 1 | PASS (markup) | Form carries `hx-trigger="change"` and `hx-on:change="...page-input...value='0'"`. Auto-refresh + page reset wiring present. Actual checkbox-click firing is owner-must-verify (see below). |

### Automated tests

- Command: `.venv/bin/python -m pytest tests/`
- Tests run: 391 — passed: 391, failed: 0
- Waiver file alone: `python -m pytest tests/test_waiver.py -q` → 25 passed.
- New tests added by me: none needed — the Engineer committed AC coverage.

AC coverage is present and non-trivial (each asserts real behaviour, both inclusion and
exclusion, against a 6-player fixture spanning every position group):
- AC1 → `test_demo_waiver_shell_renders_position_checkboxes` (asserts checkbox markup for all
  5 positions AND absence of radios / legacy `name="position"`).
- AC2 → `test_demo_waiver_post_c_lw_only_matching_positions` (asserts C/C,LW/LW included AND
  RW-only/D-only/G-only excluded).
- AC3 → `test_demo_waiver_post_d_g_union` (asserts both a D and a G present, a C excluded).
- AC4 → `test_demo_waiver_post_no_positions_spans_groups` + `..._positions_all_spans_groups`
  (both assert C/RW/D/G all present).
- Live per-position loop → `test_live_multi_position_fetches_each_position` (asserts
  `fetch_season_pool` called once per position with the right `position=` kwarg, and both
  pools merged into one table).

### Manual verification

- Ran `uvicorn web.main:app` (port 8137) and exercised the authoritative demo path directly.
- AC1: `curl /demo/waiver` → the six position inputs are all checkboxes named `positions`;
  no radios; no `name="position"`. Mutual-exclusivity `onchange` handlers present ("All"
  clears the specifics; each specific clears "All").
- AC2/AC3/AC4: POSTed to `/demo/api/waiver/players` against real demo data and parsed the
  `display_position` of every row across every page (not just page 1). Results in the table
  above. `filter_by_position` correctly splits composite `display_position` (e.g. `C,RW`) on
  comma, so dual-eligible players match on any component (verified `docs/LEARNINGS.md`
  composite-value gotcha is honoured — the route reuses `filter_by_position`, does not
  reimplement matching).

### Demo mode

Demo is the authoritative acceptance path here and is fully exercised above — the final
union filter (`pd.concat([filter_by_position(base_df, p) for p in selected])` deduped on
`player_key`) is shared by both live and demo branches, so demo parity is structural. Demo
renders end-to-end without authentication (200 on shell and both POST scenarios). Live
authenticated `/waiver` was not driven end-to-end (no Yahoo session; off-season data empty)
— the per-position fetch/cache-key loop is covered by the unit test
`test_live_multi_position_fetches_each_position` (fetch-per-position + merge), which is the
best available check without a session.

### Notes

- **Cross-position ranking artefact (out of scope, confirmed not a defect).** For AC3
  (D+G ranked by Goals, a skater stat) page 1 is all defencemen and the goalies land on
  later pages. This is the pre-existing `analysis/waiver_ranking.rank_players`
  NaN/0-composite behaviour flagged in the ticket and logged in `docs/backlog.md`. It is
  explicitly out of scope for 032 and I did not fail the ticket on it. The union logic under
  test is correct: I confirmed goalies are genuinely in the 97-row union (44 G-eligible),
  not dropped. Per ticket direction, AC3's committed test uses a fixture where D and G share
  the ranking stat, so the test is deterministic.
- **Owner-must-verify (headless limit):** I confirmed the checkbox/HTMX markup and the
  server-side normalization (`selected = [p for p in positions if p and p != "All"]`, which
  correctly handles All+specific if both were ever submitted), but did not click checkboxes
  in a real browser. The visual pill styling and the JS mutual-exclusivity toggle firing on
  a real click are owner-must-verify; behavioural correctness of the resulting POST is
  covered.
- **Out-of-scope file edits (for the Reviewer, not a QA failure):** the working tree shows
  `docs/improvements.md` and `docs/backlog.md` modified — outside the ticket's three Touches
  files. The Engineer disclosed the `improvements.md` edit in the handoff (persona input #6,
  closing a `Type: quality` item on a touched file). Flagging both for the Reviewer's scope
  check.

### Verdict: APPROVED
