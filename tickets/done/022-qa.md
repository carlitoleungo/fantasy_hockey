## QA Report — 022

**Ticket:** Logout: confirmation banner and correct redirect
**Engineer handoff:** tickets/022-done.md
**QA date:** 2026-05-30

---

### Test plan (written before reading Engineer's "How to verify")

- **AC1** — Inspect `web/routes/auth.py` logout handler; assert redirect target is `/?logged_out=1` and no branch can redirect to `/auth/login`. Also assert via test: `test_logout_valid_session_deletes_row_and_redirects` checks `location == "/?logged_out=1"`.
- **AC2** — Inspect `web/templates/home.html` for a conditional block keyed on `logged_out == "1"`. Add unit test: GET `/?logged_out=1` with a valid session returns 200 and body contains "You have been logged out."
- **AC3** — Add unit test: GET `/` (no query param) with a valid session returns 200 and body does NOT contain "You have been logged out." Also add GET `/?logged_out=0` to confirm the check is exact-value, not just presence.
- **AC4** — Inspect `web/routes/auth.py` for `delete_cookie` call on logout; confirm it was not modified. Assert via `test_logout_valid_session_deletes_row_and_redirects` that `Set-Cookie` contains `max-age=0`.
- **AC5** — Assert via `test_logout_no_cookie_redirects` that GET `/auth/logout` with no cookie returns 302 to `/?logged_out=1`. Assert via `test_logout_unknown_session_id_redirects` that an unknown session_id similarly redirects cleanly.

---

### Test results

| # | Acceptance criterion | Result | Observation |
|---|----------------------|--------|-------------|
| 1 | After clicking Logout, browser lands on `/?logged_out=1`; no OAuth round-trip | PASS | `auth.py` line 101: `RedirectResponse("/?logged_out=1", status_code=302)`. No branch in the logout handler redirects to `/auth/login`. `test_logout_valid_session_deletes_row_and_redirects` asserts `location == "/?logged_out=1"` and passes. |
| 2 | Visible banner reads "You have been logged out" when `?logged_out=1` present | PASS | `home.html` lines 7–11: `{% if request.query_params.get("logged_out") == "1" %}` wraps `<p>You have been logged out.</p>`. New test `test_home_shows_logged_out_banner_when_param_present` GET `/?logged_out=1` returns 200 with that string in body — passes. |
| 3 | Banner not shown on a normal `/` visit (no query param) | PASS | New test `test_home_no_banner_without_logged_out_param` (plain GET `/`) — "You have been logged out." absent from body — passes. New test `test_home_no_banner_when_logged_out_param_is_not_one` (GET `/?logged_out=0`) also absent — passes. The `== "1"` check is exact; `None` and `"0"` both evaluate falsy/unequal. |
| 4 | `session_id` cookie is deleted on logout | PASS | `auth.py` line 102: `response.delete_cookie("session_id", secure=secure)` — unchanged from pre-ticket state per Engineer's note. `test_logout_valid_session_deletes_row_and_redirects` asserts `"session_id" in set_cookie` and `"max-age=0" in set_cookie.lower()` — passes. `test_logout_no_cookie_still_clears_set_cookie_header` also asserts `max-age=0` when no cookie was sent — passes. |
| 5 | `GET /auth/logout` with no session cookie redirects to `/?logged_out=1` without error | PASS | `test_logout_no_cookie_redirects` asserts 302 + `location == "/?logged_out=1"` — passes. `test_logout_unknown_session_id_redirects` with a nonexistent `session_id` cookie asserts 302 + same location — passes. |

---

### Automated tests

- **Command:** `python -m pytest tests/`
- **Tests run: 329 — passed: 329, failed: 0**
- **New tests added:** `tests/test_home_routes.py` — 3 new cases (TC11, TC12, TC13) covering AC2 and AC3:
  - `test_home_shows_logged_out_banner_when_param_present`
  - `test_home_no_banner_without_logged_out_param`
  - `test_home_no_banner_when_logged_out_param_is_not_one`

  The Engineer's handoff shipped no tests for AC2/AC3 (the banner render path). The auth-route tests (AC1, AC4, AC5) were already covered by the updated `tests/test_auth_routes.py` (Tests 5–8). The banner tests were missing and have been added.

---

### Manual verification

OAuth credentials are not available in this environment. Browser walkthrough with a real Yahoo login is not possible. The following was verified by code/template inspection and automated tests; remaining items are flagged owner-must-verify.

- **Code inspection — `web/routes/auth.py`:** `logout` handler at line 94–103. Line 101 is `RedirectResponse("/?logged_out=1", status_code=302)`. No remaining reference to `/auth/login` in the logout handler. Cookie deletion at line 102 is `response.delete_cookie("session_id", secure=secure)` — correct and unchanged.
- **Code inspection — `web/templates/home.html`:** Lines 7–11 contain `{% if request.query_params.get("logged_out") == "1" %}<p ...>You have been logged out.</p>{% endif %}` placed above the `<h1>Your Leagues</h1>` heading. The conditional reads directly from `request.query_params` (Jinja2 template context has `request` in scope because the route calls `templates.TemplateResponse(request, ...)`).
- **Code inspection — `web/templates/base.html`:** Line 24 has `<a href="/auth/logout" ...>Logout</a>` — the nav link already pointed to `/auth/logout` and was not changed by this ticket. Correct.
- **Code inspection — `web/routes/home.py`:** The route handler does not pass a `logged_out` variable to the template context; the template reads `request.query_params` directly. This is the correct approach — no risk of stale server-side state.

---

### Demo mode

N/A — this ticket touches `web/routes/auth.py` and `web/templates/home.html` only. No `data/` layer functions were added or modified. Demo mode has no session and is explicitly out of scope per the ticket.

---

### Issues found

None. All acceptance criteria PASS.

**Note — missing test coverage (found during QA, now fixed):**
The Engineer did not add tests for AC2 and AC3 (the banner render path). This is not a FAIL — the implementation is correct — but the gap meant those criteria were unverified by automated tests. Three targeted tests have been added in this QA pass. The Engineer should be aware that future home-route changes affecting the query-param banner would previously have had no test catching a regression.

---

### Owner-must-verify (requires real browser + Yahoo OAuth)

1. **AC1 — No OAuth round-trip on logout:** Click Logout while logged in. Confirm browser URL becomes `/?logged_out=1` and the Yahoo consent/login screen does NOT appear. (Automated tests confirm the redirect destination; the "no OAuth round-trip" aspect requires a live browser to observe the absence of a redirect chain to Yahoo.)
2. **AC2 — Banner visible in browser:** Confirm "You have been logged out." banner is rendered and visible in the page after the logout redirect. (Template logic is verified; visual rendering requires a browser.)
3. **AC3 — Banner absent on plain `/`:** Navigate to `/` after dismissing the query param. Confirm no banner. (Verified by unit test; browser confirms visually.)
4. **Ticket Verification step 5 — re-auth flow still works:** While logged out, click "Overview". Confirm it redirects to Yahoo OAuth and back to `/` after auth completes. This is not an acceptance criterion but is a sanity check for the overall flow and requires a real Yahoo account.

---

### Verdict: APPROVED

All five acceptance criteria pass. Code inspection confirms the redirect target change is correct, the cookie deletion is unchanged, and the banner conditional in the template is logically sound. Automated test suite: 329/329 pass. Three new tests added to cover the banner render path (AC2, AC3) that the Engineer's handoff left uncovered. Two items require a real browser session for final confirmation (flagged above) but are low-risk: one is a straightforward redirect, the other is a single-line Jinja2 conditional.
