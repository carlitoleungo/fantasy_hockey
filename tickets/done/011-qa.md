## QA Report — 011 (re-verification)

**Ticket:** Landing page and league selector
**Engineer handoff:** `.team/tickets/011-done.md`, `.team/tickets/011-fix.md`
**QA date:** 2026-04-19 (initial); re-verified 2026-04-19 after fix

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `GET /` with valid session returns 200 HTML from `home.html` listing current-season leagues (season year + league name) | PASS | Returns 200; renders "2025 — Alpha League" / "2025 — Beta League"; 2024 league excluded by season filter |
| 2 | `POST /leagues/select` with valid `league_key` updates `user_sessions` row and redirects 302 to `/` | PASS | DB row updated to submitted key; `Location: /` header confirmed |
| 3 | `GET /` and `POST /leagues/select` without valid session cookie redirect to `/auth/login` (302) | PASS | Both routes return 302 with `Location: /auth/login` when cookie is absent |

---

### Bug re-verification

**Bug 1 (original): `xmltodict` missing from `requirements-web.txt`**
- **Fix applied:** `xmltodict` added to `requirements-web.txt` (confirmed at line 6 of file)
- **Re-verification:** `requirements-web.txt` now contains `xmltodict`. All 34 tests pass from a clean import (no `ModuleNotFoundError`). **RESOLVED.**

---

### Automated tests

- **Command run:** `python3.11 -m pytest tests/test_home_routes.py tests/test_auth_routes.py tests/test_error_handling.py tests/test_session_middleware.py -v`
- **Tests run:** 34 total (7 for home routes + 27 pre-existing)
- **New tests written:** `tests/test_home_routes.py` — 7 test cases (written in initial QA pass, unchanged):
  - `test_home_filters_to_current_season`
  - `test_home_empty_leagues`
  - `test_home_shows_selected_league_indicator`
  - `test_select_league_updates_db_and_redirects`
  - `test_home_no_cookie_redirects_to_login`
  - `test_select_league_no_cookie_redirects_to_login`
  - `test_home_yahoo_api_error_returns_502`
- **All passing:** YES (34/34)

---

### Manual verification

Confirmed via `TestClient` + output inspection (manual browser run requires live Yahoo credentials):

1. **Season filter:** Rendered body with 3 mock leagues (2×2025, 1×2024). The 2024 league (`Old League`) did not appear in the HTML. Both 2025 leagues (`2025 &mdash; Alpha League`, `2025 &mdash; Beta League`) appeared correctly.

2. **Selected-league indicator:** With DB `league_key = '419.l.11111'` set, the rendered HTML contained `&#10003; Selected` (checkmark) adjacent to `Alpha League` and did not show the indicator for `Beta League`. The selected league's list item carries `bg-blue-50 border-blue-300` (blue background) and the button carries `bg-blue-600 text-white`.

3. **Empty state:** With `get_user_hockey_leagues` returning `[]`, the page returned 200 with the paragraph "No active NHL leagues found for your account." — no 500.

4. **HTTPError handling:** Mocking `get_user_hockey_leagues` to raise `requests.HTTPError` produced a 502 response (routed through the existing `http_error_handler` from ticket 009), not a 500.

5. **Logout link:** `home.html` contains `<a href="/auth/logout" ...>Logout</a>` — present in template source.

6. **`POST /leagues/select` DB update:** After POST with `league_key=419.l.11111`, `SELECT league_key FROM user_sessions WHERE session_id = 'sid-test'` returned `419.l.11111`. Redirect was `302` to `/`.

7. **`requirements-web.txt` install path (re-verified):** File now lists `xmltodict` at line 6. All 34 tests load the full import chain (`home.py` → `data.leagues` → `data.client` → `xmltodict`) without error.

---

### Issues found

None. The single blocker from the initial report (Bug 1: `xmltodict` missing from `requirements-web.txt`) has been resolved.

---

### Verdict: APPROVED

All three acceptance criteria pass. All 34 automated tests pass. The blocker from the initial QA report is resolved — `xmltodict` is present in `requirements-web.txt`. No new issues found.
