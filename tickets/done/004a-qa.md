## QA Report — 004a

**Ticket:** Refactor auth/oauth.py + DB layer
**Engineer handoff:** tickets/004a-done.md
**QA date:** 2026-04-15

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `import streamlit` gone; `py_compile auth/oauth.py` succeeds without Streamlit | PASS | `grep` returns nothing; `py_compile` exits 0 with no output |
| 2 | `_save_state`, `_load_states`, `_save_states`, `_STATE_FILE` removed | PASS | `grep` finds no matches for any of the four symbols (including `_STATE_TTL`) |
| 3 | `_client_id()`, `_client_secret()`, `_redirect_uri()` read from `os.environ` | PASS | Each function body is a single `return os.environ["..."]` call; no `st.secrets` reference anywhere |
| 4 | `get_auth_url()` returns `(url: str, state: str)` tuple | PASS | Confirmed: `type(result)` is `tuple`, `len` is 2, both elements are `str`; state appears in URL params and matches the returned `state` value; `client_id` from env var is embedded in the URL |
| 5 | `db/schema.sql` contains correct DDL for `oauth_states` and `user_sessions` | PASS | Both `CREATE TABLE IF NOT EXISTS` statements present with exact columns and types matching AC (`state TEXT PRIMARY KEY, expires_at REAL` and `session_id TEXT PRIMARY KEY, access_token TEXT, refresh_token TEXT, expires_at REAL, created_at REAL`) |
| 6 | `db/connection.py` exports `get_db(path: str \| None = None) -> sqlite3.Connection` in WAL mode with `row_factory = sqlite3.Row` | PASS | `inspect.signature` confirms exact signature; functional test with a temp file confirms `journal_mode = wal`, `row_factory is sqlite3.Row`, returns `sqlite3.Connection` |
| 7 | 4 `validate_and_consume_state` tests deleted; remaining tests pass without modification | PASS | `grep` finds no trace of `validate_and_consume_state`, `MagicMock`, `sys.modules`, or `_STATE_TTL` in `tests/test_oauth_helpers.py`; 5 tests collected and all pass |
| 8 | `_try_refresh` returns `None` on failure; `st.session_state.pop` removed | PASS | No `session_state` reference anywhere in `auth/oauth.py`; functional test confirms `_try_refresh({})` returns `None` and `_try_refresh({'refresh_token': 'bad_token', ...})` returns `None` on HTTP failure |

### Automated tests

- **Command run:** `python3 -m pytest tests/ -v`
- **Tests run:** 237 tests across all test files
- **New tests written:** none needed — all acceptance criteria for this ticket are either structural (grep/compile) or covered by the existing `tests/test_oauth_helpers.py` suite; the `get_db()` functional checks were run inline during QA
- **All passing:** YES — 237 passed, 1 warning (pre-existing urllib3/OpenSSL version mismatch unrelated to this ticket)

`tests/test_oauth_helpers.py` specifically: 5 tests collected, 5 passed.

### Manual verification

This ticket has no UI component and no data-display changes, so automated + functional inline testing covers all criteria. The Streamlit app was not started — it would require live Yahoo credentials and is out of scope for a pure backend refactor with no UI changes.

**Structural checks:**
- `grep -n "import streamlit" auth/oauth.py` → no output (exit 1) ✓
- `grep -n "_save_state|_load_states|_save_states|_STATE_FILE|_STATE_TTL" auth/oauth.py` → no output ✓
- `grep -n "validate_and_consume_state|MagicMock|sys.modules|_STATE_TTL" tests/test_oauth_helpers.py` → no output ✓
- `python3 -m py_compile auth/oauth.py` → exits 0, no output ✓

**Functional checks (run without Streamlit, pure env vars):**
- `get_auth_url()` called with `YAHOO_CLIENT_ID=myid YAHOO_CLIENT_SECRET=mysecret YAHOO_REDIRECT_URI=http://localhost:8501/cb` → returned `('https://api.login.yahoo.com/oauth2/request_auth?...', '<state_token>')`. URL params contain `state`, `client_id=myid`, `response_type=code`. State in URL matches returned state value. ✓
- `get_db(path=<tmpfile>)` → `journal_mode` = `wal`, `row_factory is sqlite3.Row` = True, return type is `sqlite3.Connection` ✓
- `_try_refresh({})` → `None` (no refresh_token) ✓
- `_try_refresh({'refresh_token': 'bad_token', 'expires_at': 0})` → `None` (HTTP 401 from Yahoo token endpoint) ✓

### Issues found

None.

### Verdict: APPROVED
