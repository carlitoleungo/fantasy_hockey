# QA Report — 039 (round 2, re-verification after fix)

**Ticket:** Deployment config: fly.toml (single pinned machine, 1 GB volume) + .dockerignore
**Engineer handoff:** tickets/039-done.md (round 1), tickets/039-fix.md (round 2)
**QA date:** 2026-07-26
**Supersedes:** the round-1 NEEDS FIXES report previously at this path.

All five acceptance criteria pass. The AC4 failure from round 1 is fixed and verified over a
real build context: the context now carries 0 `__pycache__` directories and 0 `.pyc` files,
down from 7 and 32. Nothing was over-excluded, and `fly.toml` is genuinely untouched.

> The fix is a strict widening, not a trade. I probed a root-level `__pycache__/rootprobe.pyc`
> and it was excluded too, so `**/__pycache__/` still covers the root case that the literal
> `__pycache__/` covered. No root coverage was given up to gain nested coverage.

### Test plan (round 2 — no new criteria, so the round-1 plan is reused; delta noted)

- **AC4 (the failed criterion) — re-verify functionally, not by reading the file.** Rebuild a
  throwaway `FROM busybox` + `COPY . /ctx` image over the real repo-root context and count
  `.pyc`/`__pycache__`. Then a second build with probes for every pattern whose target does
  not exist on disk (root-level `__pycache__/`, nested `*.pyo`, nested `.pytest_cache/`,
  `app.db-wal`, `app.db-shm`, `.env.*`), because a passing build over a context that never
  contained the file proves nothing. Fail if any AC4-named item appears anywhere in the context.
- **AC4 regression — over-exclusion.** New this round, since the round-1 failure was an
  under-matching pattern and the fix could in principle over-match. Derive the runtime file
  set *empirically* (import `web.main`, collect every repo-local module in `sys.modules`, plus
  every file under `web/templates`, `web/static`, `demo/data`, plus `requirements-web.txt`)
  and assert each is present in the context. Then diff the whole context source tree against
  the host source tree. Fail on any missing runtime file.
- **AC1–AC3 — confirm `fly.toml` is genuinely untouched, then re-assert it anyway.** Compare
  size/mtime against the round-1 record, then re-run the full 12-assertion `tomllib` script
  rather than trusting "unchanged".
- **AC5** — `.venv/bin/python -m pytest tests/`, then boot uvicorn with `DB_PATH`/`CACHE_DIR`
  pointed at a scratch dir, `curl /health`, and confirm the DB materialises at the env path
  while repo-root `app.db` stays byte-identical.
- **Cleanup gate** — `git status` and a filesystem probe sweep must return to the pre-QA state;
  `docker images` must contain no QA image.

### Test results

| # | Acceptance criterion | Result | Observation |
|---|----------------------|--------|-------------|
| 1 | `fly.toml` exists at repo root, parses as valid TOML, sets `primary_region = "iad"` | PASS | `tomllib.load` returned without exception; `primary_region == "iad"`. Still 1587 bytes, unchanged. |
| 2 | Pins to exactly one machine; nothing that autoscales past one; a single `[[vm]]` | PASS | `min_machines_running` is int `1` (not bool, not str); `auto_start_machines is False`; `auto_stop_machines is False`; `d["vm"]` is a 1-element list. Top-level keys exactly `['app','build','env','http_service','mounts','primary_region','vm']`; `http_service` keys exactly `['auto_start_machines','auto_stop_machines','checks','force_https','internal_port','min_machines_running']` — no `max_machines_running`, no `concurrency`, no `[processes]`, no `[[services]]`, no `regions`. `grep -i -E 'autoscal\|max_machines\|processes\|regions\|concurrency\|workers\|count' fly.toml` hits only comment lines 4 and 11. |
| 3 | `[mounts]` destination `/data`; `[env]` trio; `internal_port = 8000`; health check on `/health` | PASS | `mounts == {'source': 'data', 'destination': '/data'}`. `[env]` compares **equal** to `{"DB_PATH":"/data/app.db","CACHE_DIR":"/data/cache","HTTPS_ONLY":"true"}` — three keys exactly, so no secret is smuggled in. `internal_port` is int `8000`. Exactly one check: `{method: GET, path: /health, interval: 30s, timeout: 5s, grace_period: 10s}`. 12/12 assertions passed, 0 failed. |
| 4 | `.dockerignore` excludes `.env`, `app.db`, `app.db-wal`, `app.db-shm`, `.cache/`, `.git/`, `__pycache__/` so the build context carries none of them | **PASS** (was FAIL) | Real build context: `pyc=0 pyo=0 pycache_dirs=0 pytest_cache_dirs=0`. Host has 8 `__pycache__` dirs and 64 `.pyc` files right now, so the exclusion is doing real work. `find /ctx` for all seven named items plus `__pycache__` dirs returned empty. Probe build: every planted probe excluded, including root-level. Details below. |
| 5 | `pytest tests/` green; app boots with `uvicorn web.main:app` reading `DB_PATH`/`CACHE_DIR` from env | PASS | `459 passed, 106 warnings in 1.31s`, 0 failed. App booted clean, `GET /health` → `200` `{"status":"ok"}`, `GET /` → `200`, `GET /demo/overview` → `200`. `qa2.db` (20480 bytes) created at the env `DB_PATH`; repo-root `app.db` byte-identical before and after (sha256 `8bda4d71…7ceb78`, mtime `1785076533`, 20480 bytes). |

### Automated tests

- Command: `.venv/bin/python -m pytest tests/`
- Tests run: 459 — passed: 459, failed: 0
- New tests added: **none needed** (unchanged judgement from round 1). `fly.toml` and
  `.dockerignore` are consumed by Fly and Docker, not by Python; there is no importable code
  path to unit-test and no pytest hook for build-context assertions. This is not missing AC
  coverage under DECISIONS 2026-05-31 — the criteria are not assertable from pytest at all.
  The AC evidence is the `tomllib` assertion script and the two real `docker build` runs below.

### Manual verification

**AC4 — build context, the criterion that failed in round 1.** Docker client/server 29.6.2.
Throwaway `FROM busybox` + `COPY . /ctx` Dockerfile written in scratch (outside the repo, so
it is not itself part of the context), built from the repo root with
`docker build -q -f <scratch>/qa2ctx.Dockerfile -t qa039r2ctx .` — the context is exactly what
`fly deploy` would send.

- Host baseline first, to prove the patterns have real targets: 8 `__pycache__` directories
  (`analysis auth data db tests web web/middleware web/routes`) and 64 `.pyc` files on disk,
  up from round 1's 7 and 32 (the Engineer's pytest runs added `tests/__pycache__`).
- **Build A (real context, no probes):** `pyc=0 pyo=0 pycache_dirs=0 pytest_cache_dirs=0`.
  `find /ctx -name ".env" -o -name ".env.*" -o -name "app.db" -o -name "app.db-wal" -o -name
  "app.db-shm" -o -name ".cache" -o -name ".git" -o -type d -name __pycache__` → empty. This
  is the direct reversal of the round-1 failure (7 dirs / 32 files → 0 / 0).
- **Build B (probes for patterns with no on-disk target).** `*.pyo`, nested `.pytest_cache/`,
  root-level `__pycache__/`, `app.db-wal`, `app.db-shm` and `.env.*` have no real instance, so
  Build A proves nothing about them. I planted `__pycache__/rootprobe.pyc`, `web/probe.pyo`,
  `web/routes/nested.pyo`, `web/.pytest_cache/nestedprobe`,
  `web/deep/nest/__pycache__/deep.pyc` (three levels deep), zero-byte `app.db-wal` and
  `app.db-shm`, and `.env.probe`, then rebuilt:
  `pyc=0 pyo=0 pycache_dirs=0 pytest_cache_dirs=0 dbwal=0 dbshm=0 envprobe=0`, and the
  by-name search for all five named probes returned empty.
- **The root-coverage question the Engineer flagged is settled empirically.** The root-level
  `__pycache__/rootprobe.pyc` probe was excluded, so `**/__pycache__/` matches at the root as
  well as nested. My verdict on the literal wording: AC4 names `__pycache__/` as a pattern but
  states its requirement as an *outcome* ("so a `docker build` (or `fly deploy`) build context
  carries none of them into the image"). The outcome is met, and `**/__pycache__/` is a proper
  superset of the literal form, so nothing AC4 asked for was dropped. An exact-line
  `grep -qxF '__pycache__/'` would not match, but that grep was never the criterion. **AC4 passes.**
- One cosmetic artifact worth recording: after excluding `web/deep/nest/__pycache__`, the empty
  parent directories `/ctx/web/deep` and `/ctx/web/deep/nest` still appeared in the context.
  Docker excludes the matched directory, not its ancestors. That is expected behaviour, it
  carries zero files, and it only existed because of my probe — the real repo has no such tree.

**AC4 regression — no over-exclusion.** Two independent checks, both empirical rather than a
hand-written checklist:

- Derived the runtime file set by importing `web.main` and collecting every repo-local module
  in `sys.modules`, plus every file under `web/templates`, `web/static` and `demo/data`, plus
  `requirements-web.txt`, `Dockerfile`, `fly.toml`. 55 files. Checked each against the context
  image: `CHECKED=55 MISSING_COUNT=0`. That covers all 7 `web/routes/*.py`,
  `web/middleware/session.py`, `web/templates.py`, all 12 `.html` templates, all 8 `data/*.py`,
  4 `analysis/*.py`, `auth/oauth.py`, `db/connection.py`, and all 7 `demo/data` files
  (`league_meta.json`, `stat_categories.json`, `games_remaining.json`,
  `projection_context.json`, `projection_pair_data.json`, `players_season.parquet`,
  `players_lastmonth.parquet`, `matchups.parquet`).
- Superset check: listed all 58 non-bytecode files under `web data analysis auth db demo utils`
  on the host and diffed against the same listing inside the context.
  `diff` → **identical**. The context source tree equals the host source tree minus bytecode,
  in both directions: nothing needed is missing, nothing unexpected is present.
- Top-level context listing is the same 19 entries round 1 recorded (`.dockerignore
  .python-version .streamlit CLAUDE.md Dockerfile RUNNING.md analysis app.py auth data db demo
  fly.toml pages requirements-web.txt requirements.txt utils validate_api.py web`), so no new
  top-level exclusion crept in. Whole context: 74 files, 592 KB.
- Secret sweep re-run on the new context: `find /ctx -name "*secret*" -o -name "*token*" -o
  -name "*.pem" -o -name "oauth_states.json"` returns only `/ctx/.streamlit/secrets.toml.example`,
  a committed template with no real values (same as round 1). `.streamlit/` in the context holds
  exactly `config.toml` and `secrets.toml.example`.

**Scope of the fix.** `.dockerignore` is 873 bytes, mtime 18:18:56. Its pattern list matches
round-1's enumeration item for item (`.env`, `.env.*`, the three `.streamlit` lines, `app.db`,
`app.db-wal`, `app.db-shm`, `.cache/`, `.git/`, `.gitignore`, `.venv/`, `node_modules/`,
`.DS_Store`, `*.pem`, and the non-runtime dir list `tests/ docs/ tickets/ .team/ reference/
scripts/ team-generator/ .devcontainer/ .claude/ .claudeignore WORKFLOW.md`), with only the
four `**/`-prefixed patterns and a two-line explanatory comment changed. One honest limitation:
because `.dockerignore` is untracked and was edited in place, there is **no git baseline to
byte-diff against**, so I cannot prove line-by-line that nothing else moved. I verified the
stronger property instead — the resulting context is exactly the host source tree minus
bytecode, which is what AC4 actually requires.

**AC1–AC3 — `fly.toml` genuinely untouched.** Three independent signals, not just the
Engineer's word: (a) size is still 1587 bytes, matching round 1; (b) mtime is
`Jul 26 18:07:48`, which *predates* both the round-1 handoff `tickets/039-done.md`
(18:10:22) and the fix-round `.dockerignore` edit (18:18:56) — the fix round could not have
written it; (c) I re-ran the full 12-assertion `tomllib` script anyway and every value matches
round 1 exactly. `Dockerfile` also re-confirmed unmodified: `COPY . .`, `EXPOSE 8000`, and the
single-worker `CMD ["uvicorn","web.main:app","--host","0.0.0.0","--port","8000"]` with no
`--workers`.

**AC5 — boot on env-provided paths.**
`env DB_PATH=<scratch>/bootenv/qa2.db CACHE_DIR=<scratch>/bootenv/cache HTTPS_ONLY=true
.venv/bin/python -m uvicorn web.main:app --host 127.0.0.1 --port 8151`. Startup log clean
(`Started server process` → `Waiting for application startup.` → `Application startup
complete.`), no warnings or tracebacks. `GET /health` → `200` `{"status":"ok"}` — the exact
endpoint `fly.toml`'s check targets. `GET /` → `200`. `qa2.db` (20480 bytes) appeared at the
env `DB_PATH`; repo-root `app.db` sha256 and mtime identical before and after, so the env var
is genuinely honoured rather than the app merely booting. `CACHE_DIR` was resolved but no
directory materialised, which is correct — `data/cache.py` creates it lazily on first write,
and nothing in this walk wrote to cache.

**Cleanup.** Both throwaway images removed (`docker images | grep -i qa039` → empty); scratch
Dockerfile and boot dir deleted; every probe file and directory removed (`find` sweep for all
eight probe names → empty; `__pycache__`, `web/.pytest_cache`, `web/deep` all confirmed gone).
`git status --porcelain` is byte-for-byte what it was when I started: `M docs/ROADMAP.md`,
`M docs/backlog.md`, `M tickets/039-deployment-fly-toml.md`, and untracked `.dockerignore`,
`fly.toml`, `tickets/039-{done,fix,qa}.md`, `tickets/042-*`, `tickets/043-*`, `tickets/044-*`.
The ROADMAP/backlog/042/043/044 entries are the concurrent-session noise I was told to ignore
and are not attributable to 039. `.dockerignore` and `fly.toml` hashes are unchanged by my QA
(`465f8a39…` and `c93b4d2e…`).

### Demo mode

N/A for the data layer — the ticket touches no `data/` code and adds no route. I did verify the
deployed image will not break demo mode: all 7 `demo/data` files reach the build context, and
`GET /demo/overview` returned `200` unauthenticated on the env-configured boot.

### Issues found

None. The round-1 defect is fixed and no regression was introduced.

### Owner-must-verify

Three things I could not verify here, stated plainly rather than guessed. All are carried
forward unchanged from round 1 and none is affected by the `.dockerignore` fix.

- **Fly schema acceptance.** `flyctl` is not installed (`which flyctl fly` → not found) and the
  ticket forbids `fly` commands, so `fly config validate` was not run. I verified valid TOML
  with every AC-named key present at the right name and type; I did **not** verify Fly accepts
  the schema. Run `fly config validate` as the first step of the deploy.
- **`auto_stop_machines = false` boolean form.** Current flyctl has moved this field to string
  values (`"off"`/`"stop"`/`"suspend"`), with booleans accepted as deprecated. The boolean form
  satisfies AC2 as written and cannot produce a second machine either way, but only
  `fly config validate` can confirm it isn't warned or rejected. Related: Fly documents
  `min_machines_running` as applying when machines auto-stop, so with auto-stop off it is
  likely inert. AC2 requires the key be present and it is — an observation, not a defect.
- **That the pin yields exactly one machine** is observable only post-deploy via `fly status` /
  `fly scale show`. I verified only that the config contains nothing that could produce more.
- Not owner-verify but worth naming: I built a busybox image over the context, **not** the real
  `Dockerfile` (which pip-installs pandas/pyarrow). `.dockerignore` governs the context, so
  this tests the right surface, but the shipped production image itself was never built.

### Notes

- `docs/ARCHITECTURE.md:102` still reads `fly.toml  # planned, not yet in repo` and is stale now
  that this ticket lands the file. Outside the Engineer's `Touches` and correctly not fixed by
  them. Flagging again for the Reviewer/Tech Lead to route, not filing it as a defect against 039.
- No Dockerfile change, no dependency, no CI workflow, no code change, no `docs/` change
  attributable to this round — confirmed against the tree. Diff stays inside `Touches`
  (`fly.toml`, `.dockerignore`) plus the ticket's own artifacts.
- The two unspecified `fly.toml` values remain non-conflicting, as in round 1:
  `app = "fantasy-hockey-waiver"` is named by no AC/DECISION/ROADMAP step, and the single
  `[[vm]]` (`shared`, 1 cpu, `1gb`) satisfies AC2's "a single `[[vm]]`" — DECISIONS 2026-07-23's
  "1 GB" refers to the volume, which correctly lives out-of-band in `fly volumes create`.
- No new `docs/LEARNINGS.md` entry proposed. The `.dockerignore` anchoring gotcha is real and
  general, but this repo now has exactly one `.dockerignore` with a comment at the fix site
  explaining why the `**/` prefix is load-bearing; a future ticket is unlikely to re-encounter
  it. If the Reviewer disagrees, the one-paragraph entry is theirs to add.

### Verdict: APPROVED

Status left at `qa` — a Reviewer step follows this report.
