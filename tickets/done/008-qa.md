## QA Report — 008

**Ticket:** CACHE_DIR env var
**Engineer handoff:** `.team/tickets/008-done.md`
**QA date:** 2026-04-16

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | When `CACHE_DIR` env var is set, `data/cache.py` uses its value as the cache root | PASS | `CACHE_DIR=/tmp/test_cache python3 -c "import data.cache; print(data.cache.CACHE_DIR)"` printed `/tmp/test_cache`; new test also verifies the file is written at the expected path |
| 2 | When `CACHE_DIR` is not set, module falls back to `".cache"` (existing behaviour unchanged, existing tests pass unmodified) | PASS | `python3 -c "import data.cache; print(data.cache.CACHE_DIR)"` printed `.cache`; all 25 pre-existing cache tests pass unmodified |

### Automated tests

- **Command run:** `python3 -m pytest tests/ -v --ignore=tests/test_auth_routes.py --ignore=tests/test_session_middleware.py`
- **Tests run:** 238 tests across `test_cache.py`, `test_client.py`, `test_leagues.py`, `test_matchup_sim.py`, `test_matchups.py`, `test_oauth_helpers.py`, `test_players.py`, `test_projection.py`, `test_roster.py`, `test_schedule.py`, `test_scoreboard.py`, `test_team_scores.py`, `test_waiver_ranking.py`
- **New tests written:** None needed — `test_cache_dir_env_var_overrides_default` was added by the engineer in `tests/test_cache.py` and covers the criterion fully
- **All passing:** YES (238/238)
- **Note on skipped files:** `test_auth_routes.py` and `test_session_middleware.py` fail to collect due to `ModuleNotFoundError: No module named 'fastapi'` — this is a pre-existing environment issue unrelated to this ticket. The full `python3 -m pytest tests/` command exits with code 2 due to these collection errors; that is also pre-existing and not introduced by this ticket.

### Manual verification

1. **Default fallback:** `python3 -c "import data.cache; print(data.cache.CACHE_DIR)"` → printed `.cache` ✓
2. **Env var override:** `CACHE_DIR=/tmp/test_cache python3 -c "import data.cache; print(data.cache.CACHE_DIR)"` → printed `/tmp/test_cache` ✓
3. **Code inspection:** `data/cache.py` line 19 has `import os`; line 25 has `CACHE_DIR = os.environ.get("CACHE_DIR", ".cache")`. No other logic was changed. ✓
4. **Test isolation review:** The new test in `test_cache.py` (lines 262–274) uses `monkeypatch.setenv` + `importlib.reload` to force re-evaluation of the module-level constant, asserts `cache.CACHE_DIR == custom_dir`, writes a file, checks the file exists at the expected path, and has a `finally` block that removes the env var and reloads again to restore the default — no contamination of other tests. ✓

### Issues found

None.

### Verdict: APPROVED
