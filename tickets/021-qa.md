## QA Report — 021

**Ticket:** Demo mode: /overview/head-to-head
**Engineer handoff:** tickets/021-done.md
**QA date:** 2026-05-31

---

### Test plan (written before reading Engineer's verification)

- **AC1:** Hit `GET /demo/overview/head-to-head` with no auth. Expect: 200, `<select name="team_a">` and `<select name="team_b">` dropdowns with demo team names, `<select name="from_week">` and `<select name="to_week">` selectors, initial comparison table rendered below the form.
- **AC2:** Hit `GET /demo/overview/head-to-head/table?team_a=X&team_b=Y&from_week=N&to_week=M` directly with valid demo team names. Expect: 200, partial HTML (no `<html>`/`<!DOCTYPE>`), `<table>` with sim results.
- **AC3:** Observe server logs during AC1+AC2 requests. Expect: only the two demo route log lines visible; no `yahoo.com` or Yahoo Fantasy API entries.
- **AC4:** Inspect the shell HTML for `hx-get` attribute. Expect: `hx-get="/demo/overview/head-to-head/table"`, not the authenticated path.
- **AC5:** `GET /overview/head-to-head` with no cookie. Expect: 302 redirect to `/auth/login`. Also confirm via code that authenticated handler passes `table_url="/overview/head-to-head/table"` in context so the live route renders correctly too.

---

### Test results

| # | Acceptance criterion | Result | Observation |
|---|----------------------|--------|-------------|
| 1 | `GET /demo/overview/head-to-head` returns 200 and renders shell with dropdowns and week selectors populated from demo data, no Yahoo session required | PASS | HTTP 200. `<select name="team_a">` and `<select name="team_b">` each contain 12 demo team names (e.g. `value="AHO-V-O Crew"`, `value="Dixon Dynasty"`). `<select name="from_week">` contains Week 1–14, `<select name="to_week">` the same. Initial `<table>` rendered inside `<div id="head-to-head-table">` with tally line "AHO-V-O Crew: 7 wins · Dixon Dynasty: 5 wins · 2 ties". |
| 2 | `GET /demo/overview/head-to-head/table?...` returns 200 with `_head_to_head_table.html` fragment | PASS | HTTP 200. Response begins with `<div class="overflow-x-auto">` — no `<html>` or `<!DOCTYPE>`. Contains `<table>` with team name headers, stat rows with `bg-green-100` winner cells, and tally row. |
| 3 | No outbound Yahoo API calls made when hitting either demo route | PASS | Second uvicorn instance started fresh on port 8001; both demo routes hit; server log contained only `"GET /demo/overview/head-to-head HTTP/1.1" 200 OK` and `"GET /demo/overview/head-to-head/table?... HTTP/1.1" 200 OK`. No Yahoo API calls logged. Automated test TC-D10 and TC-D11 confirm `make_session` and live `get_matchups` are never called. |
| 4 | Changing team/week in demo shell updates table via HTMX against `/demo/overview/head-to-head/table` | PASS | Shell HTML contains `hx-get="/demo/overview/head-to-head/table"` and `hx-target="#head-to-head-table"` on the `<form>` element. The authenticated fragment URL `/overview/head-to-head/table` does not appear in the demo shell. Automated test TC-D05 confirms. |
| 5 | Authenticated `/overview/head-to-head` still uses live data route unchanged | PASS | `GET /overview/head-to-head` (no cookie) → HTTP 302 `location: /auth/login`. Authenticated route with mock session + `TestClient` renders `hx-get="/overview/head-to-head/table"` (not the demo URL). All 4 render sites for `head_to_head.html` in `overview.py` provide `table_url`. |

---

### Automated tests

- **Command:** `source .venv/bin/activate && python -m pytest tests/`
- **Pre-existing tests:** 332 passed, 0 failed
- **Full suite (including new tests):** 345 passed, 0 failed
- **New tests added:** `tests/test_demo_head_to_head_routes.py` (13 tests)
  - TC-D01: `/demo/overview/head-to-head` returns 200 without auth
  - TC-D02: Team dropdowns populated from demo data
  - TC-D03: Week selectors populated from demo data
  - TC-D04: Initial comparison table rendered in shell
  - TC-D05: `hx-get` targets demo fragment URL, not authenticated URL
  - TC-D06: Fragment endpoint returns partial HTML (no `<html>` tag)
  - TC-D07: Fragment contains tally (wins/ties)
  - TC-D08: Fragment contains winner highlighting (`bg-green-100`)
  - TC-D09: `not_enough_data` guard renders empty-state message for <2 teams
  - TC-D10: `make_session` and live `get_matchups` not called on demo shell
  - TC-D11: `make_session` and live `get_matchups` not called on demo fragment
  - TC-D12: `from_week > to_week` swapped correctly (same output as normal order)
  - TC-D13: Authenticated `/overview/head-to-head` still redirects to login

---

### Manual verification

- Started FastAPI server (already running on port 8000, confirmed via `GET /health` → `{"status":"ok"}`).
- `curl -s -w "%{http_code}" http://localhost:8000/demo/overview/head-to-head` → **200**. Inspected HTML: two team dropdowns with 12 real demo team names, week selectors Week 1–14, initial sim table rendered.
- `curl -s http://localhost:8000/demo/overview/head-to-head | grep hx-get` → `hx-get="/demo/overview/head-to-head/table"` confirmed.
- `curl -s "http://localhost:8000/demo/overview/head-to-head/table?team_a=AHO-V-O+Crew&team_b=Dixon+Dynasty&from_week=1&to_week=5"` → **200**, response starts with `<div class="overflow-x-auto">`, contains `<table>`, `bg-green-100`, wins/ties tally. No `<html>` present.
- Started a fresh uvicorn on port 8001 and captured logs during both demo route requests — only those two GET lines visible; no Yahoo API activity.
- `curl -sD - http://localhost:8000/overview/head-to-head | grep -E "^HTTP|^location"` → `HTTP/1.1 302 Found` / `location: /auth/login`.
- Ran `TestClient` inline to confirm authenticated shell renders `hx-get="/overview/head-to-head/table"` (live, not demo).
- Grepped all render sites for `head_to_head.html` in `web/routes/overview.py` — all 4 call sites provide `table_url`.

---

### Demo mode

- Visited `http://localhost:8000/demo/overview/head-to-head` — page renders fully without authentication. Matched live-mode equivalent in shape: same template, same dropdowns, same table layout.
- Visited fragment endpoint directly — returns partial `_head_to_head_table.html` with demo data. Matches shape of authenticated fragment.
- Demo shell uses `"Demo League"` as `selected_league_name` in the header.
- Demo routes import `data.demo` inline; no session dependency, no `_get_league_key` call.

---

### Issues found

None.

---

### Verdict: APPROVED
