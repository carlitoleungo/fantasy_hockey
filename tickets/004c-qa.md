## QA Report — 004c

**Ticket:** Test file cleanups (remove dead import, replace magic number, move inline import)
**Engineer handoff:** tickets/004c-done.md
**QA date:** 2026-04-16

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `date` removed from `from datetime import ...` in `test_matchups.py`; `datetime`, `timedelta`, `timezone` unchanged | PASS | Line 11 reads `from datetime import datetime, timedelta, timezone` — no `date` present |
| 2 | `test_is_valid_returns_false_within_buffer` uses `TOKEN_EXPIRY_BUFFER_SECONDS - 1`; constant imported from `auth.oauth` at module level | PASS | Line 9 imports `TOKEN_EXPIRY_BUFFER_SECONDS`; line 43 uses `time.time() + TOKEN_EXPIRY_BUFFER_SECONDS - 1` — no hardcoded `30` |
| 3 | `from analysis.projection import _is_rate_stat` moved to top-level imports; no import statement inside `test_is_rate_stat` body | PASS | Import is on line 6 at module level; `test_is_rate_stat` function body contains only assertions |
| 4 | All 34 tests pass | PASS | 34 passed, 0 failed, 1 urllib3 ssl warning (pre-existing, unrelated to this ticket) |

### Automated tests

- Command run: `python3 -m pytest tests/test_matchups.py tests/test_oauth_helpers.py tests/test_projection.py -v`
- Tests run: 34 total — 12 in test_matchups.py, 5 in test_oauth_helpers.py, 17 in test_projection.py
- New tests written: none needed — ticket is purely cleanup, existing tests cover all changed code
- All passing: YES

### Manual verification

This ticket touches only test files — no UI components, no data display functions, no demo mode paths. Manual app verification is not applicable.

Spot-checked each file directly:

**test_matchups.py:** Read line 11. Import line is `from datetime import datetime, timedelta, timezone`. Confirmed `date` is absent. Grep for `date` in the import block found no matches.

**test_oauth_helpers.py:** Read full file. `TOKEN_EXPIRY_BUFFER_SECONDS` is imported on line 9 alongside `_is_valid` and `_stamp_expiry`. `test_is_valid_returns_false_within_buffer` (line 41–44) sets `expires_at` to `time.time() + TOKEN_EXPIRY_BUFFER_SECONDS - 1` and asserts `_is_valid` returns `False`. No hardcoded `30` in the file.

**test_projection.py:** Read top-level imports (lines 5–6): `from analysis import projection` and `from analysis.projection import _is_rate_stat`. Read `test_is_rate_stat` function body (lines 219–226): contains only `assert` statements, no import.

### Issues found

None.

### Verdict: APPROVED
