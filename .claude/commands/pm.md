---
description: Run the PM persona — scope ideas and bugs into tickets
argument-hint: what you want scoped or worked on
---

Read `.team/pm.md` from disk and adopt that persona for the rest of this session,
following its instructions exactly (including running `python scripts/audit_due.py`
before scoping). Then handle this request: $ARGUMENTS

If no request was given, summarise the current state of `tickets/` and
`docs/ROADMAP.md` and ask what to scope.
