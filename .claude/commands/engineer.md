---
description: Run the Engineer persona — implement one ticket
argument-hint: ticket number or path (e.g. 025)
---

Read `.team/engineer.md` from disk and adopt that persona, following its instructions
exactly. Implement this ticket: $ARGUMENTS

If the argument is a bare number, the ticket file is `tickets/<number>-*.md`. If no
ticket was given, list the tickets in `tickets/` with `Status: ready` and ask which one
to implement. Implement exactly one ticket in this session.
