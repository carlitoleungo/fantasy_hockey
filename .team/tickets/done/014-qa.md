## QA Report — 014

**Ticket:** Nav shell in base.html
**Engineer handoff:** `.team/tickets/014-done.md`
**QA date:** 2026-04-20

---

### My test plan (written before reading engineer's "How to verify")

| # | Acceptance criterion | My planned test |
|---|---------------------|-----------------|
| 1 | `base.html` renders `<header>` with (a) app-name link to `/`, (b) optional league label, (c) logout link to `/auth/logout` | Read template source; render page and extract `<header>` HTML; assert all three elements present |
| 2 | Logout link removed from `home.html`; "Your Leagues" heading remains | Read `home.html` source; check no `/auth/logout` in content block; confirm `<h1>` heading present |
| 3 | `home.py` passes `selected_league_name` into context, derived from leagues list, no extra API calls | Read `home.py`; verify `next(...)` derivation pattern; confirm only `get_user_hockey_leagues` called once; confirm key present in `TemplateResponse` dict |
| 4 | GET / with selected league renders league name in header; without selected league renders no label and no layout break | Run TC9 (selected) and TC10 (unselected); also extract `<header>` from rendered HTML and inspect directly |
| 5 | Unit test asserts header contains app-name anchor and logout link | Run TC8 and read its assertions |

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `base.html` renders `<header>` with app-name link, optional league label, logout link | PASS | Header appears at lines 13–22 of `base.html`, before `{% block content %}`. All three elements present: `<a href="/">Fantasy Hockey</a>`, `{% if selected_league_name %}` conditional with `&middot;` separator and `text-gray-500` span, `<a href="/auth/logout">Logout</a>` |
| 2 | Logout link removed from `home.html`; "Your Leagues" heading remains | PASS | `home.html` contains no reference to `/auth/logout`. The content block opens directly with `<h1 class="text-2xl font-bold text-gray-800 mb-6">Your Leagues</h1>` |
| 3 | `home.py` passes `selected_league_name`, derived from leagues list, no extra API call | PASS | `home.py` lines 33–36 use `next(...)` over the already-fetched `leagues` list. Only one call to `get_user_hockey_leagues`. `selected_league_name` passed in the `TemplateResponse` context dict |
| 4a | GET / with selected league renders league name in header | PASS | Rendered `<header>` HTML confirmed: `<span class="text-gray-500 text-sm">Alpha League</span>` present between app-name and logout link |
| 4b | GET / with no selected league renders header without league label, no layout break | PASS | Rendered `<header>` HTML with `league_key=None` has no `&middot;` and no league name span. Header still renders cleanly with just app name and logout |
| 5 | Unit test asserts header has app-name anchor and logout link | PASS | TC8 (`test_home_renders_nav_header`) asserts `href="/"`, `"Fantasy Hockey"`, and `'<a href="/auth/logout"'` all present in response body |

---

### Automated tests

- **Command run:** `.venv/bin/python -m pytest tests/ -v`
- **Tests run:** 276
- **New tests written:** None needed — TC8/TC9/TC10 were written by the engineer as part of this ticket and directly cover the acceptance criteria. All prior tests (TC1–TC7) continue to pass.
- **All passing:** YES — 276 passed, 0 failed, 19 warnings (all DeprecationWarnings from Starlette test client cookie handling, pre-existing)

---

### Manual verification

**Setup:** used TestClient with in-memory SQLite and mocked `get_user_hockey_leagues` to render actual template output and inspect HTML sections.

**With selected league (league_key = "419.l.11111"):**
- Extracted `<header>...</header>` from rendered HTML.
- Confirmed `<a href="/" class="font-semibold text-gray-800 hover:text-gray-600">Fantasy Hockey</a>` present.
- Confirmed `<span class="text-gray-400">&middot;</span>` and `<span class="text-gray-500 text-sm">Alpha League</span>` present inside the header's left div.
- Confirmed `<a href="/auth/logout" class="text-sm text-gray-500 hover:text-gray-700">Logout</a>` present on the right.
- `&middot;` present in full body: True.

**Without selected league (league_key = None):**
- Extracted `<header>...</header>` from rendered HTML.
- Header rendered correctly: app name + logout, no separator, no league name span.
- `&middot;` present in full body: False (confirmed the conditional guard works).
- `/auth/logout` in page content section (below header): False — logout link only appears in the header, not in the content block.

**Error page (`error.html` extending `base.html`):**
- Triggered a 502 by having `get_user_hockey_leagues` raise `requests.HTTPError`.
- `error.html` renders with the shared header (no `selected_league_name` in its context).
- Jinja2 treats the undefined variable as falsy — the `{% if selected_league_name %}` block is silently skipped. Header renders cleanly: `Fantasy Hockey | Logout`, no separator, no crash.

**Regression — "Select" buttons:**
- TC4 (`test_select_league_updates_db_and_redirects`) confirms POST `/leagues/select` still writes to DB and returns 302. PASS.

**Note on TC9 assertion scope:** `test_home_header_shows_selected_league_name` asserts `"Alpha League" in response.text`, but "Alpha League" also appears in the league list body (since it's one of the returned leagues). The test does not isolate that the name appears specifically within the `<header>` element. This is not a defect — the behavior is correct and verified manually above — but a future improvement to TC9 could tighten the assertion (e.g., extract the header substring). Flagging as a minor observation, not a blocker.

**Missing `.team/LEARNINGS.md`:** The file was referenced in the test engineer instructions but does not exist in this repo. No impact on this ticket — noted for the team.

---

### Issues found

None.

---

### Verdict: APPROVED
