## Implementation complete — 009 (QA fix)

**What I did:**
- Fixed both `TemplateResponse` calls in `web/main.py` to use the Starlette 1.0.0 API: `request` is now the first positional argument, and the context dict no longer includes `"request"`.

**Files changed:**
- `web/main.py` — Updated `http_error_handler` (lines 20–24) and `internal_error_handler` (lines 29–33): changed `TemplateResponse("error.html", {"request": request, "detail": ...}, status_code=N)` to `TemplateResponse(request, "error.html", {"detail": ...}, status_code=N)`.

**How to verify:**
- Run `.venv/bin/python -m pytest tests/test_error_handling.py -v` — all 7 tests should pass.
- All 7 previously failing tests now pass; prior suite unaffected.

**Scope notes:**
- QA noted that ticket 011 also references the old `TemplateResponse` calling convention in its notes — that will need updating before 011 ships.

**Known limitations:**
- None.
