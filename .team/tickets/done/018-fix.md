## Implementation complete — 018 (QA fix)

**What I did:**
- Fixed Bug 1: demo route now derives `stat_cols` from `demo_module.get_matchups()` instead of `demo_module.load_season_pool()`, so `stat_columns()` receives a matchups-style DataFrame and correctly excludes metadata columns.
- Updated TC4 mock to patch `data.demo.get_matchups` instead of `data.demo.load_season_pool`, so the test exercises the actual code path introduced by the fix.

**Files changed:**
- `web/routes/waiver.py` — `demo_waiver_shell`: replaced `load_season_pool()` call with `get_matchups()` call for building `stat_cols`.
- `tests/test_waiver_routes.py` — TC4: updated patch target from `data.demo.load_season_pool` to `data.demo.get_matchups`.

**How to verify:**
- Run `python -m pytest tests/test_waiver_routes.py -v` — all 7 tests pass.
- Start server with `uvicorn web.main:app --port 8765`, then `curl http://localhost:8765/demo/waiver | grep 'type="checkbox"'` — should show only real stat names (Goals, Assists, etc.), not `player_key`, `player_name`, `team_abbr`, `display_position`, or `status`.

**Scope notes:**
- No changes outside the two files above.

**Known limitations:**
- None.
