## QA Report — 002

**Ticket:** Expand data/analysis layer test coverage
**Engineer handoff:** `.team/tickets/002-done.md`
**QA date:** 2026-04-14

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `tests/test_oauth_helpers.py` exists, all tests pass, no direct `streamlit` import | PASS | 8 tests collected and passing; `grep -n "^import streamlit\|^from streamlit"` returns nothing; only references are a docstring comment (line 5), the `sys.modules["streamlit"] = MagicMock()` patch (line 17), and the string `.streamlit/` in a comment (line 72) |
| 1a | Covers `_stamp_expiry` adds `expires_at ≈ time.time() + expires_in` | PASS | `test_stamp_expiry_adds_expires_at` asserts `before + 3600 <= result["expires_at"] <= after + 3600` |
| 1b | Covers `_is_valid` returns `False` when `expires_at` is in the past | PASS | `test_is_valid_returns_false_when_expired` sets `expires_at = time.time() - 100` |
| 1c | Covers `_is_valid` returns `False` within the 60-second buffer | PASS | `test_is_valid_returns_false_within_buffer` sets `expires_at = time.time() + 30` |
| 1d | Covers `_is_valid` returns `True` when more than 60 seconds remain | PASS | `test_is_valid_returns_true_when_ample_time_remains` sets `expires_at = time.time() + 3600` |
| 1e | Covers `validate_and_consume_state` returns `False` for unknown nonce | PASS | `test_validate_unknown_nonce_returns_false` |
| 1f | Covers `validate_and_consume_state` returns `True` once then `False` (one-time use) | PASS | `test_validate_known_nonce_true_then_false` calls twice and asserts `True` then `False` |
| 1g | Covers `validate_and_consume_state` returns `False` for expired nonce | PASS | `test_validate_expired_nonce_returns_false` seeds JSON with `time.time() - 1` |
| 2 | `tests/test_matchups.py` gains two new tests covering both re-fetch directions | PASS | `test_prev_week_refetched_when_cache_updated_today` and `test_prev_week_not_refetched_when_cache_updated_yesterday` both present and passing |
| 2a | Today path: asserts prev week is re-fetched when cache written today | PASS | Patches `cache.last_updated` to `datetime.now(utc)`, seeds week 4, current_week=5; asserts `4 in fetched_weeks` and `5 in fetched_weeks` |
| 2b | Yesterday path: asserts prev week is NOT re-fetched when cache written yesterday | PASS | Patches `cache.last_updated` to `now - timedelta(days=1)`; asserts `4 not in fetched_weeks` and `5 in fetched_weeks` |
| 2c | Both use `monkeypatch` on `cache.last_updated` and `client.get_all_teams_week_stats` | PASS | Both tests use `monkeypatch.setattr` on both; cache seeding goes through autouse `isolated_cache` fixture (redirected to `tmp_path`) |
| 3 | `tests/test_projection.py` gains `test_is_rate_stat` testing True/False for all required values | PASS | `_is_rate_stat` importable directly; asserts `True` for `"GAA"`, `"SV%"`, `"Goals Against Average"`, `"Save Percentage"`; asserts `False` for `"Goals"`, `"Assists"` |
| 4 | `pytest tests/` passes with zero failures and zero errors, count strictly higher than baseline | PASS | Baseline: 229 tests (no `test_oauth_helpers.py`); after: 240 tests, all passing, zero errors |

---

### Automated tests

- **Command run:** `python3 -m pytest tests/ -v`
- **Tests run:** 240 (all test files across `tests/`)
- **New tests written by QA:** none needed — all acceptance criteria covered by the engineer's implementation
- **All passing:** YES — `240 passed, 1 warning in 2.53s`

Targeted runs also executed:
- `python3 -m pytest tests/test_oauth_helpers.py -v` — 8 passed
- `python3 -m pytest tests/test_matchups.py -v -k "refetch"` — 2 passed
- `python3 -m pytest tests/test_projection.py::test_is_rate_stat -v` — 1 passed

Baseline verified by stashing ticket changes: git stash revealed 229 tests collected with a collection error on `test_oauth_helpers.py` (caused by the missing `from __future__ import annotations` in `auth/oauth.py` — which the engineer's fix resolves). Post-ticket count of 240 is strictly higher.

---

### Manual verification

This ticket adds test coverage only; the one production-file change (`auth/oauth.py`) is a one-line `from __future__ import annotations` addition at the top of the imports block — confirmed via `git diff HEAD -- auth/oauth.py`. The diff shows no logic changes.

No UI changes were made, so full Streamlit manual walkthrough is not required. However:

- Confirmed `from __future__ import annotations` is present at `auth/oauth.py:21` (first import line, after the module docstring).
- Confirmed the auth module is importable in Python 3.9 by virtue of all 8 `test_oauth_helpers.py` tests running cleanly (they import `_stamp_expiry`, `_is_valid`, `validate_and_consume_state`, `_STATE_TTL` from `auth.oauth` via the MagicMock patch).
- The LEARNINGS.md file referenced in the QA brief does not exist — `.team/LEARNINGS.md` is absent from the repository. This is a documentation gap but not a blocker for this ticket.

---

### Issues found

None.

---

### Verdict: APPROVED
