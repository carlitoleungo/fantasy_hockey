## QA Report — 010

**Ticket:** make_session helper and league_key schema migration
**Engineer handoff:** tickets/010-done.md
**QA date:** 2026-04-17

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `make_session(access_token)` returns a `requests.Session` whose `Authorization` header is `Bearer {access_token}` | PASS | `auth/oauth.py:51–55` — function confirmed; `test_make_session_sets_authorization_header` passed; `session.headers["Authorization"] == "Bearer test_token"` verified |
| 2 | `user_sessions` table includes `league_key TEXT` (nullable) — verifiable via `schema.sql` and fresh test DB | PASS | `db/schema.sql:12` has `league_key TEXT` with no `NOT NULL`; `pragma_table_info` on fresh DB shows `league_key\|TEXT\|0`; INSERT without `league_key` stores NULL as expected |
| 3 | All existing tests continue to pass unmodified | PASS | 239 tests pass; 3 FastAPI tests error on import due to missing `fastapi` module — pre-existing, unrelated to this ticket |

**Migration idempotency (from Notes for QA):** PASS — running `ALTER TABLE user_sessions ADD COLUMN league_key TEXT` twice on the same DB raises `sqlite3.OperationalError: duplicate column name: league_key` on the second run; the try/except in `web/main.py:21–22` catches it silently. No unhandled exception.

**lifespan vs on_event (Tech Lead flag):** PASS — `web/main.py:15–25` uses the `@asynccontextmanager async def lifespan(app)` pattern; deprecated `@app.on_event` is not used.

### Automated tests

- Command run: `python3 -m pytest tests/test_oauth_helpers.py -v` and `python3 -m pytest tests/ --ignore=tests/test_auth_routes.py --ignore=tests/test_error_handling.py --ignore=tests/test_session_middleware.py`
- Tests run: 6 in `test_oauth_helpers.py`; 239 total (non-FastAPI suite)
- New tests written: none needed — `test_make_session_sets_authorization_header` was added by the engineer and covers AC1 directly
- All passing: YES (239/239; 3 FastAPI import errors are pre-existing)

### Manual verification

1. **Inspected `auth/oauth.py`** — `make_session` is defined at lines 51–55 in the Public API section; imports `requests` (already present); sets `Authorization: Bearer {token}` via `session.headers.update`. Matches the spec exactly.

2. **Inspected `db/schema.sql`** — `user_sessions` CREATE TABLE statement includes `league_key TEXT` as the last column, no `NOT NULL` constraint. Matches the spec.

3. **Fresh DB check** — ran `sqlite3 /tmp/qa_010_fresh.db < db/schema.sql` then `SELECT name, type, "notnull" FROM pragma_table_info('user_sessions')`. Output confirmed all 6 columns including `league_key|TEXT|0`.

4. **Nullable check** — `INSERT INTO user_sessions (session_id, access_token) VALUES ('test-sess', 'tok123')` on the fresh DB succeeded; `SELECT league_key` returned empty string (NULL). Nullable confirmed.

5. **Idempotency check** — created a legacy DB (no `league_key` column), ran ALTER TABLE twice in Python. First run: `OK`. Second run: caught `OperationalError: duplicate column name: league_key`. No unhandled exception raised.

6. **`web/main.py` lifespan** — migration runs inside `@asynccontextmanager async def lifespan(app)` assigned at `FastAPI(lifespan=lifespan)`. Tech Lead's flag on `on_event` does not apply — engineer used the correct modern approach.

7. **`improvements.md`** — engineer correctly closed the `TOKEN_EXPIRY_BUFFER_SECONDS` item; the existing test already used the constant. No spurious changes to the open items.

### Issues found

None.

### Observation (non-blocking)

The ticket's "Notes for the engineer" specified placing `make_session` *below* `exchange_code` in the Public API section; the engineer placed it *above* `exchange_code`. Both are in the Public API block. This has no functional impact and is not an acceptance criterion — noted for awareness only, not a defect.

### Verdict: APPROVED
