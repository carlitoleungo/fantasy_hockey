## QA Report — 018 (re-verification after fix)

**Ticket:** Waiver Wire: shell and filter controls
**Engineer handoff:** `.team/tickets/018-done.md` (initial) / `.team/tickets/018-fix.md` (fix)
**QA date:** 2026-04-30

---

### Summary of re-verification

The original report issued NEEDS FIXES for one Major bug: demo route rendered player
metadata columns (`player_key`, `player_name`, `team_abbr`, `display_position`, `status`)
as stat chip checkboxes. The engineer fixed this by replacing `demo_module.load_season_pool()`
with `demo_module.get_matchups()` as the source for `stat_cols` in `demo_waiver_shell`, and
updated TC4's mock target to match. All issues from the original report have been re-verified.

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `GET /waiver` returns 200 HTML with page title, 6 position pills, period radio, one stat chip per `stat_columns(matchups_df)` entry | PASS | TC1 confirms; manual verified in original run |
| 2 | All controls in `<form id="waiver-filters" hx-post="/api/waiver/players" hx-target="#waiver-table-container" hx-trigger="change">` | PASS | TC5 confirms; `hx-post`, `hx-target`, `hx-trigger` all present in manual curl |
| 3 | `<div id="waiver-table-container">` with empty-state message "Select one or more stat categories above to rank available players." | PASS | TC6 + manual curl confirmed both `id` and exact text present |
| 4 | `GET /waiver` without selected league → 302 to `/`; without session → 302 to `/auth/login` | PASS | TC2/TC3 pass; `curl /waiver` → 302 to `http://localhost:8765/auth/login`; bogus session → same |
| 5 | `GET /demo/waiver` renders shell; stat columns from `demo.get_matchups()` via `stat_columns()`; form action `hx-post="/demo/api/waiver/players"` | **PASS (fixed)** | `waiver.py` line 84 now calls `demo_module.get_matchups()`. `curl /demo/waiver \| grep 'type="checkbox"'` returns only real stat names (Goals, Assists, Points, etc.). No metadata columns present. |
| 6 | Bug 1 (original): metadata columns as stat chips | **RESOLVED** | `grep -E 'player_key\|player_name\|team_abbr\|display_position\|status'` returns zero lines on live response |
| 7 | `base.html` gains "Waiver" nav link after "Overview" | PASS | `<a href="/waiver" ...>Waiver</a>` present; confirmed in live response |
| 8 | Unit test: `GET /waiver` with 3×2×3 fixture → all 6 positions + 3 stats present | PASS | TC1 passes; TC4 now patches `data.demo.get_matchups` (not `load_season_pool`) — exercises the actual fixed code path |

---

### Automated tests

- **Command run:** `source .venv/bin/activate && python -m pytest tests/ -v`
- **Tests run:** 307
- **New tests written:** none needed — TC4 was updated by engineer to patch `data.demo.get_matchups`; existing 7 tests in `tests/test_waiver_routes.py` cover all criteria
- **All passing:** YES — 307/307

---

### Manual verification

Server started: `uvicorn web.main:app --port 8765`

**Bug 1 re-verification (`GET /demo/waiver`):**

1. `curl http://localhost:8765/demo/waiver | grep 'type="checkbox"'` → 14 checkboxes, all real stat names: Goals, Assists, Points, Plus/Minus, Penalty Minutes, Powerplay Points, Shots on Goal, Faceoffs Won, Hits, Blocks, Wins, Goals Against Average, Saves, Shutouts
2. `grep -E 'player_key|player_name|team_abbr|display_position|status'` on same response → empty output ✓ (metadata columns no longer appear)

**Full shell controls (`GET /demo/waiver`):**

3. `href="/waiver"` in nav ✓
4. `hx-post="/demo/api/waiver/players"`, `hx-target="#waiver-table-container"`, `hx-trigger="change"` all present on `<form>` ✓
5. 6 position radio inputs: All, C, LW, RW, D, G ✓
6. 2 period radio inputs: Season, Last 30 days ✓
7. `<div id="waiver-table-container">` with "Select one or more stat categories above to rank available players." ✓

**Redirect checks:**

8. `curl -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:8765/waiver` → `302 http://localhost:8765/auth/login` ✓
9. `curl -o /dev/null -w "%{http_code} %{redirect_url}" http://localhost:8765/waiver -H "Cookie: session_id=bogus"` → `302 http://localhost:8765/auth/login` ✓

---

### Issues found

None. Bug 1 from the original report is resolved. No regressions observed.

---

### Verdict: APPROVED

All 8 acceptance criteria pass. Bug 1 (metadata columns as stat chips in demo route) is fully resolved — the fix is correct, the test mock now exercises the actual code path, and the live response confirms only real stat names appear as checkboxes. 307/307 tests pass.
