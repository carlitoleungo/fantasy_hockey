## QA Report — 001 (Re-review)

**Ticket:** Tech Lead: stack selection + ARCHITECTURE.md
**Engineer handoff:** `.team/tickets/001-done.md`
**QA date:** 2026-04-14

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `docs/ARCHITECTURE.md` exists and is committed to the repo | PASS | Present on disk; `git log` shows commit `8a88ec5` |
| 2 | Names exactly one web framework and argues why it is preferred over at least two named alternatives for this specific use case (multi-tenant, Yahoo OAuth callback, Python data stack, single-engineer maintained) | PASS | Decisions log now has a dedicated row: "FastAPI over Flask or Django as the backend framework." Flask and Django are named backend alternatives. Rationale cites concurrent OAuth callbacks (multi-tenant), Python data stack compatibility (Pydantic), and single-engineer maintenance (OpenAPI docs). Both Flask and Django are given specific rejection reasons. |
| 3 | Storage backend named for each of 3 tiers: CSRF state nonces with TTL, encrypted OAuth tokens per user, per-user matchups/players cache | PASS | (1) SQLite `oauth_states` table at `/data/app.db`; `expires_at = now + 300 s`; one-time delete. (2) SQLite `user_sessions` table at `/data/app.db`; 30-day cookie `Max-Age`. (3) Local disk via `data/cache.py` at `/data/cache/{league_key}/` on Fly.io persistent volume. All specific. |
| 4 | Deployment target stated (platform, region, containerised vs. managed) with ≥1 alternative and reasoning | PASS | Fly.io, region `iad`, 1 container + 1 persistent volume. Alternatives: Railway (rejected — less mature persistent volume support); AWS ECS (rejected — excessive overhead for a single engineer). |
| 5 | Preserved table covers every file under `data/`, `analysis/`, `auth/oauth.py`, `demo/data/`; Replaced table covers every file under `app.py` and `pages/`; all listed paths exist | PASS | All three previously missing files now present: `data/__init__.py` in Preserved; `analysis/__init__.py` in Preserved; `pages/__init__.py` in Replaced. Cross-referenced against `ls data/ analysis/ pages/` — every file accounted for. |
| 6 | Session strategy describes how identity travels from OAuth callback through to route handlers; mechanism and lifetime named | PASS | Mechanism: server-side session; `session_id` in `HttpOnly; Secure; SameSite=Lax` cookie. Flow: callback validates nonce → `session_id` generated → row inserted in `user_sessions` → cookie set → `require_user` dependency reads cookie on each request → looks up tokens → calls `_is_valid`/`_try_refresh` → injects `CurrentUser` into `request.state`. Lifetime: `Max-Age=2592000` (30 days). No internal contradictions. |

### Automated tests

- Command run: N/A — this ticket produces a document, not code
- Tests run: none applicable
- New tests written: none needed
- All passing: N/A

### Manual verification

1. Confirmed `docs/ARCHITECTURE.md` present and tracked (`git log docs/ARCHITECTURE.md` → `8a88ec5`).
2. Read the full document top to bottom.

**AC2 verification:** Located the decisions log. Found a new dedicated row for "FastAPI over Flask or Django as the backend framework" (distinct from the existing HTMX vs React/Vue row). Confirmed: alternatives are Flask and Django — both backend frameworks, not frontend stacks. Rationale explicitly names Yahoo OAuth callbacks, Python data stack, and single-engineer maintenance as the drivers. The previous HTMX/frontend comparison row is still present as a supplementary entry but the backend framework decision now stands on its own.

**AC5 verification:** Ran `ls data/ analysis/ pages/`:
- `data/`: `__init__.py`, `cache.py`, `client.py`, `demo.py`, `leagues.py`, `matchups.py`, `players.py`, `roster.py`, `schedule.py`, `scoreboard.py` (10 files + `__pycache__`)
- `analysis/`: `__init__.py`, `matchup_sim.py`, `projection.py`, `team_scores.py`, `waiver_ranking.py` (5 files + `__pycache__`)
- `pages/`: `__init__.py`, `01_league_overview.py`, `03_waiver_wire.py`, `04_week_projection.py` (4 files + `__pycache__`)

Cross-referenced against both tables in `docs/ARCHITECTURE.md`. Every file maps to either the Preserved or Replaced table. The three previously-missing `__init__.py` files are all now present in their correct tables.

**AC3, AC4, AC6:** Re-verified against current document text. No regressions from the prior passing results. Storage tiers table, deployment section, and session strategy section are unchanged and still satisfy their criteria.

**Notes for QA criteria (from original ticket):** (a) Every decision has ≥1 sentence of justification ✓; (b) all listed file paths exist on disk ✓; (c) no internal contradictions — server-side session used consistently throughout, tokens remain in DB ✓.

### Issues found

None.

### Verdict: APPROVED

Both previously-failed criteria are resolved. AC2 now argues FastAPI against named backend alternatives (Flask, Django) with reasoning explicitly tied to the stated use-case constraints. AC5 now includes all three `__init__.py` files in their correct tables. All six acceptance criteria pass. No regressions in previously-passing criteria.
