## Implementation complete — 011 (QA fix)

**What I did:**
- Added `xmltodict` to `requirements-web.txt`

**Files changed:**
- `requirements-web.txt` — added `xmltodict`; without it the app crashed on startup because `web/routes/home.py` imports `get_user_hockey_leagues`, which chains to `data.client`, which imports `xmltodict` at module level

**How to verify:**
- `pip install -r requirements-web.txt && uvicorn web.main:app --reload` — server should start without `ModuleNotFoundError`

**Scope notes:**
- None

**Known limitations:**
- None
