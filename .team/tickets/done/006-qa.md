## QA Report — 006

**Ticket:** Update CLAUDE.md for the FastAPI stack
**Engineer handoff:** tickets/006-done.md
**QA date:** 2026-04-11

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `## Streamlit-Specific Patterns` section removed in its entirety | PASS | Section does not exist anywhere in the file |
| 2 | `## Tech Stack` replaced with single pointer line to `docs/ARCHITECTURE.md` | PASS | Line 17–19: exactly `See \`docs/ARCHITECTURE.md\` for stack decisions.` — no bullets, no extra content |
| 3 | `## Architecture` directory tree replaced with single pointer line | PASS | Line 13–15: exactly `See \`docs/ARCHITECTURE.md\` for directory structure and layer rules.` |
| 4 | `## Running Locally` covers both stacks with correct subheadings and commands | PASS | Lines 86–98: `### Streamlit prototype` → `streamlit run app.py`; `### FastAPI app` → `uvicorn web.main:app --reload` |
| 5 | Intro paragraph contains no mention of Streamlit as the stack | PASS | Line 4: "A public-facing web app that helps fantasy hockey managers evaluate waiver wire add/drop decisions…" — no Streamlit in the opening sentence or anywhere in the intro |
| 6 | Known Gotchas, Testing Strategy, Caching Strategy, Key Decisions Log pointer, Out of Scope, and Secrets & Auth unchanged | PASS | All six sections present and intact; `@st.cache_data` and `st.secrets` references in preserved sections are expected survivors |

### Automated tests
- Command run: `/Users/carlin/dev/fantasy_hockey/.venv/bin/pytest tests/ -v`
- Tests run: 229 (test_cache, test_client, test_leagues, test_matchup_sim, test_matchups, test_players, test_projection, test_roster, test_schedule, test_scoreboard, test_team_scores, test_waiver_ranking)
- New tests written: none needed — this is a documentation-only change, no data or analysis functions were modified
- All passing: YES — 229 passed in 6.44s

### Manual verification

Opened `CLAUDE.md` (111 lines) and read top to bottom.

**Intro paragraph (line 4):** "A public-facing web app that helps fantasy hockey managers evaluate waiver wire add/drop decisions using Yahoo Fantasy API data. Users authenticate with their own Yahoo account; the backend fetches their league, matchup, and player data; the frontend renders stat tables and rankings. A demo mode lets unauthenticated users explore a pre-snapshotted dataset." — Streamlit is not named as the stack. The suggested replacement text from the Tech Lead review matches exactly.

**`## Architecture` (lines 13–15):** Single line: `See \`docs/ARCHITECTURE.md\` for directory structure and layer rules.` No directory tree, no code block.

**`## Tech Stack` (lines 17–19):** Single line: `See \`docs/ARCHITECTURE.md\` for stack decisions.` No bullet list.

**`## Streamlit-Specific Patterns`:** Absent. Searched file; not present.

**`## Running Locally` (lines 86–98):** Two subheadings present:
- `### Streamlit prototype` with `pip install -r requirements.txt` then `streamlit run app.py`
- `### FastAPI app` with `pip install -r requirements-web.txt` then `uvicorn web.main:app --reload`
Both commands match the ticket spec exactly.

**Preserved sections spot-check:**
- `## Caching Strategy` (lines 21–50): full table and prose intact
- `## Secrets & Auth` (lines 52–62): `st.secrets["yahoo"]["client_id"]` reference intact — expected per ticket spec
- `## Known Gotchas` (lines 72–78): `@st.cache_data` on line 73 and "default Streamlit port is 8501" on line 74 — both stale but preserved per AC6; engineer's done file explicitly defers these to a follow-up ticket
- `## Testing Strategy` (lines 80–84): unchanged
- `## Key Decisions Log` (lines 103–104): pointer to `docs/decisions.md` unchanged
- `## Out of Scope` (lines 106–111): unchanged

**`st.` reference check (per QA notes in ticket):** Two `st.` references exist outside the `## Secrets & Auth` section:
- Line 73 (`@st.cache_data`) and line 74 ("Streamlit port is 8501") — both in `## Known Gotchas`, which AC6 explicitly preserves unchanged. These are flagged for a follow-up ticket by the engineer and are not a failure here.

**Note on `web-ext lint`:** The QA prompt asked to run `web-ext lint` and "load the extension". `web-ext` is a Mozilla browser extension CLI tool and is not relevant to this project (a Python/FastAPI/Streamlit fantasy hockey app). There is no browser extension component. This step was skipped; it appears to be a copy-paste error in the QA instructions.

### Issues found

None.

### Verdict: APPROVED
