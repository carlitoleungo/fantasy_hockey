## QA Report — 009 (re-verify after fix)

**Ticket:** Base template and error handling
**Engineer handoff:** `.team/tickets/009-done.md`, fix notes `.team/tickets/009-fix.md`
**QA date:** 2026-04-16

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `GET /health` returns `{"status": "ok"}` | PASS | `test_health_still_returns_ok` — 200, correct JSON body |
| 2 | Route raising `requests.HTTPError` returns 502 HTML containing "Something went wrong" / recognisable error message | PASS | Returns 502, `text/html` content-type, body contains "Yahoo API request failed" |
| 3 | `base.html` includes Tailwind, HTMX, Alpine CDN tags and defines `{% block title %}`, `{% block head %}`, `{% block content %}` | PASS | All three CDN URLs and all three block declarations confirmed present |

---

### Re-verification of original Bug #1

**Bug #1 — `TemplateResponse` called with old Starlette API (both exception handlers broken)**

Fix confirmed applied. `web/main.py` lines 20–25 and 29–34 now use:
```python
templates.TemplateResponse(request, "error.html", {"detail": "..."}, status_code=N)
```
Both handlers are fully functional.

---

### Automated tests

- **Command run:** `.venv/bin/python -m pytest tests/test_error_handling.py -v`
- **Tests run:** 7 (all in `tests/test_error_handling.py`)
- **Results:**
  - `test_health_still_returns_ok` — PASS
  - `test_http_error_returns_502` — PASS
  - `test_http_error_returns_html` — PASS
  - `test_http_error_contains_yahoo_message` — PASS
  - `test_500_handler_fires_for_bare_exception` — PASS
  - `test_500_handler_returns_html` — PASS
  - `test_500_handler_contains_error_message` — PASS
- **Prior suite:** `.venv/bin/python -m pytest tests/ --ignore=tests/test_error_handling.py -q` — **258 passed, 0 failures**, no regressions
- **All passing:** YES

---

### Manual verification

**AC1 — /health smoke test**
`test_health_still_returns_ok` calls `GET /health` via `TestClient`. Response: 200, `{"status": "ok"}`. PASS.

**AC2 — HTTPError exception handler**
`test_http_error_returns_502` adds a test route raising `requests.HTTPError("Yahoo said no")` and calls it with `raise_server_exceptions=False`. Observed: status 502, `content-type: text/html; charset=utf-8`, body contains "Yahoo API request failed". Previously this returned an empty 500 with no body. Fix confirmed effective.

`test_http_error_returns_html` confirms `content-type` header is `text/html; charset=utf-8`. PASS.

`test_http_error_contains_yahoo_message` confirms body text contains "Yahoo API request failed". PASS.

**Additional — 500 handler for bare exceptions**
`test_500_handler_fires_for_bare_exception` raises bare `Exception("boom")`. Status 500 returned. PASS.

`test_500_handler_returns_html` confirms `content-type: text/html; charset=utf-8`. PASS.

`test_500_handler_contains_error_message` confirms body contains "Something went wrong". PASS.

**AC3 — base.html structure**
`web/main.py` imports without error; `web/templates/base.html` confirmed unchanged:
- TailwindCSS: `<script src="https://cdn.tailwindcss.com">` ✓
- HTMX: `<script src="https://unpkg.com/htmx.org@2.0.4">` ✓
- Alpine.js: `<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js">` ✓
- `{% block title %}` ✓
- `{% block head %}` ✓
- `{% block content %}` ✓

---

### Issues found

None. All previously failing tests now pass. No regressions in the prior suite.

---

### Verdict: APPROVED

All three acceptance criteria pass. Bug #1 (Starlette 1.0.0 `TemplateResponse` API mismatch) is resolved — both exception handlers now return HTML responses with the correct status codes and error messages. 265 total tests passing, 0 failures.
