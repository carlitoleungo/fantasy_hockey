## QA Report — 004d

**Ticket:** Move `_db_dep` to `db/connection.py`
**Engineer handoff:** `.team/tickets/done/004d-done.md`
**QA date:** 2026-04-16

---

### Test plan (written before reading "How to verify")

| # | Criterion | What I did | Pass condition |
|---|-----------|-----------|----------------|
| 1a | `db_dep()` defined in `db/connection.py`, public | Code inspection | Function present, no underscore |
| 1b | No `import sqlite3` inside `db_dep()` body | Code inspection | Zero occurrences inside function |
| 1c | `db_dep()` calls `get_db()`, yields, has `finally` | Code inspection | All three present |
| 2a | `_db_dep` not defined in `auth.py` | Grep across codebase | No match in source files |
| 2b | Both `login` and `callback` use `Depends(db_dep)` | Code inspection | Two occurrences confirmed |
| 2c | `auth.py` imports `db_dep` from `db.connection` | Code inspection | Import line present |
| 3a | Test file imports `db_dep` from `db.connection` | Code inspection | Line 18 confirmed |
| 3b | `dependency_overrides` key uses `db_dep` | Code inspection | Line 57 confirmed |
| 3c | All four auth-route tests pass | `python -m pytest tests/ -v` | 4/4 pass |
| extra | No circular import | `python -c "from db.connection import db_dep"` | No error |

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | `db/connection.py` exports public `db_dep()` that calls `get_db()`, yields, closes in `finally`; no `import sqlite3` in the body | PASS | `db_dep` at lines 17–23; `import sqlite3` is a module-level import for `get_db`, not inside `db_dep`. No `import sqlite3` appears inside the function body. |
| 2 | `web/routes/auth.py` does not define `_db_dep`; `login` and `callback` both use `Depends(db_dep)` imported from `db.connection` | PASS | No `_db_dep` definition anywhere in `auth.py`. Import at line 25: `from db.connection import db_dep`. `Depends(db_dep)` at lines 34 and 45. |
| 3 | `tests/test_auth_routes.py` imports `db_dep` from `db.connection` as the `dependency_overrides` key; all four tests pass without test-logic changes | PASS | Import at line 18: `from db.connection import db_dep`. Override key at line 57: `app.dependency_overrides[db_dep] = override_db`. All 4 tests pass. |

---

### Automated tests

- **Command run:** `.venv/bin/python -m pytest tests/ -v`
- **Tests run:** 241 tests across 14 test files
- **Auth-route tests specifically:** `test_login_redirects_to_yahoo_and_stores_nonce`, `test_callback_invalid_state_returns_400_no_yahoo_call`, `test_callback_valid_nonce_creates_session`, `test_callback_nonce_one_time_use` — all 4 PASSED
- **New tests written:** None needed — existing test suite fully covers the acceptance criteria; the ticket is a mechanical refactor with no logic changes.
- **All passing:** YES — 241/241

---

### Manual verification

1. Inspected `db/connection.py`:
   - `from typing import Generator` present at line 12
   - `db_dep()` defined at line 17, docstring reads "FastAPI dependency: open a DB connection and close it after the response."
   - Body: `conn = get_db()` → `try: yield conn` → `finally: conn.close()` — correct pattern
   - No `import sqlite3` inside the function body (module-level `import sqlite3` at line 11 is appropriate and used by `get_db`)

2. Inspected `web/routes/auth.py`:
   - No `_db_dep` function definition anywhere in the file
   - Line 25: `from db.connection import db_dep` — correct
   - Line 34: `def login(db=Depends(db_dep)):` — correct
   - Line 45: `def callback(request: Request, db=Depends(db_dep)):` — correct

3. Inspected `tests/test_auth_routes.py`:
   - Line 18: `from db.connection import db_dep` — correct
   - Line 57: `app.dependency_overrides[db_dep] = override_db` — correct

4. Grepped for `_db_dep` across the entire codebase: only appears in ticket/docs/done files (expected). Zero occurrences in `db/connection.py`, `web/routes/auth.py`, or `tests/test_auth_routes.py`.

5. Verified import in a subprocess: `python -c "from db.connection import db_dep"` — clean, no circular import errors. Output: `<function db_dep at 0x...>`

6. Ran full test suite — 241 tests, 0 failures, 0 errors.

---

### Issues found

None.

---

### Verdict: APPROVED
