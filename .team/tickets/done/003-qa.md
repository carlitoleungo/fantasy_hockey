## QA Report — 003

**Ticket:** New project scaffold
**Engineer handoff:** tickets/003-done.md
**QA date:** 2026-04-15

---

### Test plan (written before reading engineer's "How to verify")

| # | Acceptance criterion | My planned check |
|---|---------------------|-----------------|
| 1 | Manifest installs cleanly in clean venv | `python3 -m venv .venv-web && .venv-web/bin/pip install -r requirements-web.txt`; expect zero errors |
| 2 | Server starts; `/health` returns 200 + `"ok"` | `uvicorn web.main:app --port 8000`; curl health endpoint; verify status code and body |
| 3 | Folder structure matches ARCHITECTURE.md skeleton | File-by-file existence check for entry-point, routes dir, middleware dir, db placeholder, auth placeholder |
| 4 | Streamlit app unharmed | `streamlit run app.py`; verify 200 response; grep web/ for forbidden imports |

---

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | Manifest exists at ARCHITECTURE.md path; installs cleanly in clean venv | PASS | `pip install -r requirements-web.txt` in fresh `.venv-web` (Python 3.11) completed without errors. All five declared packages installed: fastapi, uvicorn[standard], jinja2, itsdangerous, python-multipart |
| 2 | Start command starts server without errors; `/health` returns 200 with `"ok"` in body | PASS | `uvicorn web.main:app --port 8000` started cleanly. `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/health` returned `200`. `curl -s http://localhost:8000/health` returned `{"status":"ok"}` |
| 3 | Folder structure matches ARCHITECTURE.md skeleton (entry-point, routes dir, auth placeholder) | PASS | `web/main.py` (entry-point), `web/routes/` with `__init__.py` + `health.py`, `web/middleware/__init__.py`, `web/templates/.gitkeep`, `web/static/.gitkeep`, `db/__init__.py` all present. `auth/` pre-exists from prototype (unchanged). Deferred items (`db/connection.py`, `db/schema.sql`, `web/middleware/session.py`, `fly.toml`, template HTML files, other route stubs) are correctly out-of-scope per the Tech Lead review and the ticket's "minimum" phrasing |
| 4 | `streamlit run app.py` still starts without errors | PASS | App started and responded HTTP 200 over HTTPS (`:8501`). No errors in startup output. `grep -r 'import.*app\|from app\|from pages\|import pages' web/` returned no matches |

---

### Automated tests

- **Command run:** `.venv/bin/python -m pytest tests/ -v`
- **Tests run:** 240 tests across 13 test files
- **New tests written:** None needed — ticket 003 creates infrastructure scaffolding and a single trivial route (`return {"status": "ok"}`). There is no new data-layer or analysis logic requiring fixture-based tests.
- **All passing:** YES — 240 passed in 3.13s, 0 failures, 0 errors

---

### Manual verification

**AC1 — Clean venv install:**
Created `.venv-web` using `.venv/bin/python3 -m venv .venv-web` (Python 3.11, matching the Dockerfile). Ran `.venv-web/bin/pip install -r requirements-web.txt`. All packages resolved and installed without errors. Output ended with `Successfully installed fastapi-0.135.3 ... uvicorn-0.44.0 ...`. The venv was removed after testing.

**AC2 — Health endpoint:**
Started server: `.venv-web/bin/uvicorn web.main:app --port 8000 --no-access-log`. Startup log showed `Application startup complete.` and `Uvicorn running on http://127.0.0.1:8000`. From a second shell:
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/health` → `200`
- `curl -s http://localhost:8000/health` → `{"status":"ok"}`

Both the status code criterion and the body string `"ok"` are satisfied.

**AC3 — Folder structure:**
Verified every file listed in the engineer's handoff exists. Cross-checked against ARCHITECTURE.md's directory listing. The three "at minimum" items the ticket explicitly requires are all present:
- Entry-point file: `web/main.py` ✓
- Routes directory: `web/routes/` (with `__init__.py` and `health.py`) ✓
- Auth-layer placeholder directory: `auth/` (pre-existing, untouched) ✓

Items listed in ARCHITECTURE.md but absent (`db/schema.sql`, `db/connection.py`, `web/middleware/session.py`, `fly.toml`, Jinja2 templates, other route stubs) are all ticket-004a or later scope per the Tech Lead review note. Not a defect for this ticket.

**AC4 — Streamlit unharmed:**
`streamlit run app.py` started without errors. Startup log showed the expected Streamlit splash (Local/Network/External URLs). `curl -sk -o /dev/null -w "%{http_code}" https://localhost:8501/` returned `200`. Confirmed the app uses HTTPS (Streamlit default with a self-signed cert); curl `-k` flag bypasses cert validation for local testing.

Ran `grep -r 'import.*app\|from app\|from pages\|import pages' web/` — no matches. The scaffold is fully isolated from Streamlit files.

**Note on LEARNINGS.md:** `.team/LEARNINGS.md` does not exist. The pre-QA instruction says to read it. Flagging for awareness — no learnings file to consult for this QA run.

---

### Issues found

None.

---

### Verdict: APPROVED
