## QA Report — 012

**Ticket:** Local-dev DB bootstrap and env documentation
**Engineer handoff:** `.team/tickets/012-done.md`
**QA date:** 2026-04-19

### Test results

| # | Acceptance criterion | Result | Notes |
|---|---------------------|--------|-------|
| 1 | Fresh boot (no DB_PATH, no app.db) → server reaches "Application startup complete" | PASS | Confirmed: uvicorn started cleanly; no errors in log; `GET /health` → 200 |
| 2 | `_DEFAULT_DB_PATH` changed to `./app.db`; new default added to `.gitignore` | PASS | Line 15 of `db/connection.py` is `_DEFAULT_DB_PATH = "./app.db"`; `.gitignore` lines 19–21 cover `app.db`, `app.db-wal`, `app.db-shm`; docstring at line 31 updated to reference `./app.db` and Fly.io override |
| 3 | `init_db(conn)` in `db/connection.py` runs `db/schema.sql` via `executescript`; idempotent | PASS | Function exists at line 43–46; uses `_SCHEMA_PATH.read_text()` + `executescript`; no Python "exists" guards — idempotency comes from `CREATE TABLE IF NOT EXISTS` in `schema.sql` as specified |
| 4 | `web/main.py` lifespan calls `init_db(conn)` before `ALTER TABLE` block | PASS | Line 20: `init_db(conn)` is first call inside `try:`; `ALTER TABLE` follows at line 22 |
| 5 | `.env.example` documents all 6 env vars with comments | PASS | All 6 vars present: `DB_PATH`, `CACHE_DIR`, `HTTPS_ONLY`, `YAHOO_CLIENT_ID`, `YAHOO_CLIENT_SECRET`, `YAHOO_REDIRECT_URI`; each has a `#` comment describing purpose and default |
| 6 | Tech Lead add: `.env` also in `.gitignore` | PASS | `.gitignore` line 22: `.env` present |

### Automated tests

- Command run: `.venv/bin/python3 -m pytest tests/ -v`
- Tests run: 273 tests across 16 test files
- New tests written: none needed — ticket only touches bootstrap/config code; no new data or analysis functions introduced
- All passing: YES (273 passed, 0 failed)

**Note — pre-existing environment issue:** Before running the suite, `python-multipart` was missing from the venv, causing 3 collection errors. This is not introduced by ticket 012 — `python-multipart` is correctly listed in `requirements-web.txt`. The venv had simply not been updated to include a dependency added by ticket 011. I installed it manually and all tests passed. The engineer should ensure the venv is rebuilt from `requirements-web.txt` before running tests.

### Manual verification

**Test 1 — Fresh boot:**
1. Confirmed `app.db` did not exist.
2. Ran `uvicorn web.main:app --reload --port 8001` with no `DB_PATH` set.
3. Log output: `INFO: Application startup complete.` — no errors.
4. `curl http://localhost:8001/health` → `{"status":"ok"}`.

**Test 2 — Schema check after fresh boot:**
1. Ran `sqlite3 ./app.db ".schema"`.
2. Output shows both `oauth_states` and `user_sessions` tables.
3. `user_sessions` includes `league_key TEXT` column — confirms both `init_db` and the `ALTER TABLE` path executed correctly (ALTER raises `OperationalError` because `league_key` already exists in the schema, swallowed as expected).

**Test 3 — Idempotency (restart with existing DB):**
1. Restarted uvicorn without deleting `app.db`.
2. Log: `INFO: Application startup complete.` — no errors.
3. Schema unchanged.

**Test 4 — Nested path creation:**
1. Confirmed `./var/` did not exist.
2. Ran `DB_PATH=./var/test.db uvicorn web.main:app --port 8002`.
3. Server started cleanly; `ls ./var/` showed `test.db` created.
4. Cleaned up `./var/` after test.

**Test 5 — `.env.example` in git; `app.db` gitignored:**
1. `git status` after boot showed `app.db` absent from tracked/untracked list — correctly ignored.
2. `.env.example` is listed as `??` (untracked new file, as expected for a just-added file not yet committed).
3. No `.env` file present in repo.

### Issues found

None.

### Verdict: APPROVED
